(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (re.++ (re.++ re.allchar (str.to_re "aaa")) (str.to_re "bbb")))))
(assert (not (str.in_re S (re.++ (str.to_re "bbbaaa") re.allchar))))
(check-sat)
