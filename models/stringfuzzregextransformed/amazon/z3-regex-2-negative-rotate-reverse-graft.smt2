(set-logic QF_S)
(declare-const S String)
(assert (str.in.re S (re.++ re.allchar (re.++ (re.++ re.allchar (str.to.re "aaa")) (str.to.re "bbb")))))
(assert (not (str.in.re S (re.++ (str.to.re "bbbaaa") re.allchar))))
(check-sat)
