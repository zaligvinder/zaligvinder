(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "&&&&&&ssssss") re.allchar)))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "ssssss") (re.++ (str.to_re "&&&&&&") re.allchar))))))
(check-sat)
