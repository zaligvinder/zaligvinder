(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ (str.to_re "aaabbb") re.allchar))))
(assert (str.in_re S (re.++ re.allchar (re.++ (str.to_re "bbb") (re.++ (str.to_re "aaa") re.allchar)))))
(check-sat)
