(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ re.allchar (str.to_re "[[[[[[KKKKKK")))))
(assert (str.in_re S (re.++ re.allchar (re.++ (str.to_re "[[[[[[") (re.++ re.allchar (str.to_re "KKKKKK"))))))
(check-sat)
