(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ re.allchar (str.to_re "UUU---")))))
(assert (str.in_re S (re.++ re.allchar (re.++ (re.++ (str.to_re "---") (str.to_re "UUU")) re.allchar))))
(check-sat)
