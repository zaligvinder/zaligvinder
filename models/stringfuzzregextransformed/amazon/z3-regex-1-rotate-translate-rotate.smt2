(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ (str.to_re "```]]]") re.allchar))))
(assert (str.in_re S (re.++ (str.to_re "```") (re.++ (re.++ re.allchar (str.to_re "]]]")) re.allchar))))
(check-sat)
