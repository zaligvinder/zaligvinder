(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "NNNqqq") re.allchar)))
(assert (not (str.in_re S (re.++ (re.++ (re.++ (str.to_re "NNN") re.allchar) (str.to_re "qqq")) re.allchar))))
(check-sat)
