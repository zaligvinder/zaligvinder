(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ (re.++ (str.to_re "bbb") (re.++ (str.to_re "aaa") re.allchar)) re.allchar))))
(assert (str.in_re S (re.++ re.allchar (str.to_re "aaabbb"))))
(check-sat)
