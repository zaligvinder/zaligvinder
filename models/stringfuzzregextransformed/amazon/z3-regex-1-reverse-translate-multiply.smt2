(set-logic QF_S)
(declare-const S String)
(assert (not (str.in.re S (re.++ re.allchar (str.to.re "UUUUUU------")))))
(assert (str.in.re S (re.++ re.allchar (re.++ (str.to.re "UUUUUU") (re.++ re.allchar (str.to.re "------"))))))
(check-sat)
