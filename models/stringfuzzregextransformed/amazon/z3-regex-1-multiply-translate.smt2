(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ (str.to_re "oooooocccccc") re.allchar))))
(assert (str.in_re S (re.++ (re.++ (re.++ (str.to_re "oooooo") re.allchar) (str.to_re "cccccc")) re.allchar)))
(check-sat)
