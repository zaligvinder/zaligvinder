(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "lll###") re.allchar)))
(assert (not (str.in_re S (re.++ (re.++ (str.to_re "###") (re.++ (str.to_re "lll") re.allchar)) re.allchar))))
(check-sat)
