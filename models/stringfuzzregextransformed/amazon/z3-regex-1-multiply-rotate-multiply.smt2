(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ (str.to_re "aaaaaaaaaaaabbbbbbbbbbbb") re.allchar))))
(assert (str.in_re S (re.++ re.allchar (re.++ (str.to_re "bbbbbbbbbbbb") (re.++ (str.to_re "aaaaaaaaaaaa") re.allchar)))))
(check-sat)
