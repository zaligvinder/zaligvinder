(set-logic QF_S)
(declare-const S String)
(assert (str.in.re S (re.++ (str.to.re "LLLppp") re.allchar)))
(assert (not (str.in.re S (re.++ re.allchar (re.++ (str.to.re "ppp") (re.++ (str.to.re "LLL") re.allchar))))))
(check-sat)
