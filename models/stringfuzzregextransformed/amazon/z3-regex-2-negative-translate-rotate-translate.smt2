(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "LLLppp") re.allchar)))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "ppp") (re.++ (str.to_re "LLL") re.allchar))))))
(check-sat)
