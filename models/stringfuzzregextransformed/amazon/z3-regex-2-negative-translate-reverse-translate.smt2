(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "NNNYYY"))))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "NNN") (re.++ re.allchar (str.to_re "YYY")))))))
(check-sat)
