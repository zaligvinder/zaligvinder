(set-logic QF_S)
(declare-const S String)
(assert (str.in.re S (re.++ re.allchar (str.to.re "NNNYYY"))))
(assert (not (str.in.re S (re.++ re.allchar (re.++ (str.to.re "NNN") (re.++ re.allchar (str.to.re "YYY")))))))
(check-sat)
