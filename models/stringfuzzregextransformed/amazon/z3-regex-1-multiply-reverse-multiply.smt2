(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ re.allchar (str.to_re "bbbbbbbbbbbbaaaaaaaaaaaa")))))
(assert (str.in_re S (re.++ re.allchar (re.++ (str.to_re "bbbbbbbbbbbb") (re.++ re.allchar (str.to_re "aaaaaaaaaaaa"))))))
(check-sat)
