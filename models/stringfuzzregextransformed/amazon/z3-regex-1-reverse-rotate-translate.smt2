(set-logic QF_S)
(declare-const S String)
(assert (not (str.in.re S (re.++ re.allchar (str.to.re "www&&&")))))
(assert (str.in.re S (re.++ re.allchar (re.++ (re.++ (str.to.re "&&&") (str.to.re "www")) re.allchar))))
(check-sat)
