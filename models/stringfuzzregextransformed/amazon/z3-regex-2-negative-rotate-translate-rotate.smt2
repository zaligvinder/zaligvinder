(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "'\\n''\\n''\\n'YYY") re.allchar)))
(assert (not (str.in_re S (re.++ (str.to_re "'\\n''\\n''\\n'") (re.++ (re.++ re.allchar (str.to_re "YYY")) re.allchar)))))
(check-sat)
