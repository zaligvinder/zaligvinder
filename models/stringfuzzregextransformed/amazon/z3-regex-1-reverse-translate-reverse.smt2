(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ (str.to_re "---UUU") re.allchar))))
(assert (str.in_re S (re.++ (re.++ (re.++ (str.to_re "---") re.allchar) (str.to_re "UUU")) re.allchar)))
(check-sat)
