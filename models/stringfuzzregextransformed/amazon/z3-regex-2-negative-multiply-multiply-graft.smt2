(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (re.++ (str.to_re "aaaaaaaaaaaa") re.allchar) re.allchar)))
(assert (not (str.in_re S (re.++ (re.++ (str.to_re "aaaaaaaaaaaabbbbbbbbbbbb") (str.to_re "bbbbbbbbbbbb")) re.allchar))))
(check-sat)
