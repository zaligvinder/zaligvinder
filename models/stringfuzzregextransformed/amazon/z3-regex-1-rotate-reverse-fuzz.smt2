(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.union re.allchar (str.to_re "b$'\u{b}'%@")))))
(assert (str.in_re S (re.++ (re.++ (re.++ re.allchar (str.to_re "a")) (str.to_re "bbb")) re.allchar)))
(check-sat)
