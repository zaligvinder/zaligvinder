(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "///```"))))
(assert (not (str.in_re S (re.++ (re.++ (re.++ re.allchar (str.to_re "```")) (str.to_re "///")) re.allchar))))
(check-sat)
