(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "bbbbbbaaaaaa"))))
(assert (not (str.in_re S (re.++ (re.++ (re.++ re.allchar (str.to_re "aaaaaa")) (str.to_re "bbbbbb")) re.allchar))))
(check-sat)
