(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "YYY'n\\''n\\''n\\'"))))
(assert (not (str.in_re S (re.++ (re.++ (re.++ re.allchar (str.to_re "'n\\''n\\''n\\'")) (str.to_re "YYY")) re.allchar))))
(check-sat)
