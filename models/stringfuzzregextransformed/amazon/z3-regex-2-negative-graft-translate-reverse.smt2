(set-logic QF_S)
(declare-const S String)
(assert (str.in.re S (re.++ re.allchar (str.to.re "###lll"))))
(assert (not (str.in.re S (re.++ re.allchar (re.++ (re.++ re.allchar (str.to.re "lll")) (str.to.re "###"))))))
(check-sat)
