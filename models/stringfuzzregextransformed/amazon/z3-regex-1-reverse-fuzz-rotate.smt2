(set-logic QF_S)
(declare-const S String)
(assert (not (str.in.re S (re.++ re.allchar (str.to.re "b7l;_\\\\$0la")))))
(assert (str.in.re S (re.++ re.allchar (re.++ (re.++ (str.to.re "}/c") (str.to.re "")) re.allchar))))
(check-sat)
