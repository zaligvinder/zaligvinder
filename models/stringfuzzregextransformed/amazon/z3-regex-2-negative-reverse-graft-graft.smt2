(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (str.to_re "aaa")))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "bbb") (re.++ re.allchar (str.to_re "bbbaaa")))))))
(check-sat)
