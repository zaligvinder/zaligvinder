(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "iiiEEE"))))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "iii") (re.++ re.allchar (str.to_re "EEE")))))))
(check-sat)
