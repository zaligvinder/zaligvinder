(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ re.allchar (str.to_re "---MMM")))))
(assert (str.in_re S (re.++ re.allchar (re.++ (re.++ re.allchar (str.to_re "MMM")) (str.to_re "---")))))
(check-sat)
