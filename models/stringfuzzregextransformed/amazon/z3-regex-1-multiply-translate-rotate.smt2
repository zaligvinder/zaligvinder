(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ (str.to_re "oooooocccccc") re.allchar))))
(assert (str.in_re S (re.++ re.allchar (re.++ (str.to_re "cccccc") (re.++ (str.to_re "oooooo") re.allchar)))))
(check-sat)
