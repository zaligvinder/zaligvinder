(set-logic QF_S)
(declare-const S String)
(assert (str.in.re S (re.++ re.allchar (str.to.re "$$$LLL"))))
(assert (not (str.in.re S (re.++ re.allchar (re.++ (re.++ (str.to.re "LLL") (str.to.re "$$$")) re.allchar)))))
(check-sat)
