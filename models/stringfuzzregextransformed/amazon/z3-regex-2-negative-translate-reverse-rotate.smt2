(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "$$$LLL"))))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (re.++ (str.to_re "LLL") (str.to_re "$$$")) re.allchar)))))
(check-sat)
