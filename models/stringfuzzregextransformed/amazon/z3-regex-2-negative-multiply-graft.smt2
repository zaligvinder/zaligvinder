(set-logic QF_S)
(declare-const S String)
(assert (str.in.re S (str.to.re "bbbbbb")))
(assert (not (str.in.re S (re.++ (re.++ (re.++ (str.to.re "aaaaaa") re.allchar) (re.++ (str.to.re "aaaaaabbbbbb") re.allchar)) re.allchar))))
(check-sat)
