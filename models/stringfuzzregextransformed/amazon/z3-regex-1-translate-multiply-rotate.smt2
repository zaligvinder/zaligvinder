(set-logic QF_S)
(declare-const S String)
(assert (not (str.in.re S (re.++ (str.to.re "MMMMMM------") re.allchar))))
(assert (str.in.re S (re.++ re.allchar (re.++ (str.to.re "------") (re.++ (str.to.re "MMMMMM") re.allchar)))))
(check-sat)
