(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "EEEiii") re.allchar)))
(assert (not (str.in_re S (re.++ (re.++ (re.++ (str.to_re "EEE") re.allchar) (str.to_re "iii")) re.allchar))))
(check-sat)
