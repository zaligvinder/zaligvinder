(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (str.to_re "$$$$$$")))
(assert (not (str.in_re S (re.++ (re.++ (re.++ (str.to_re "LLLLLL") re.allchar) (re.++ (str.to_re "LLLLLL$$$$$$") re.allchar)) re.allchar))))
(check-sat)
