(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "h)+gYC0.g1L") re.allchar)))
(assert (not (str.in_re S (re.++ (re.++ (re.++ (str.to_re "h~%b") re.allchar) (str.to_re "S")) re.allchar))))
(check-sat)
