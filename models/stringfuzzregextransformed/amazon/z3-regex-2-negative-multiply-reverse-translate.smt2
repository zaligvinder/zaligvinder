(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "ssssss||||||"))))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "ssssss") (re.++ re.allchar (str.to_re "||||||")))))))
(check-sat)
