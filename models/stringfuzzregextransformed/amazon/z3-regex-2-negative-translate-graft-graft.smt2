(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (str.to_re "$$$")))
(assert (not (str.in_re S (re.++ (re.++ (re.++ (str.to_re "LLL") re.allchar) (str.to_re "LLL$$$")) re.allchar))))
(check-sat)
