(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (re.++ (str.to_re "aaaaaa") re.allchar) re.allchar)))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "bbbbbb") (str.to_re "aaaaaabbbbbb"))))))
(check-sat)
