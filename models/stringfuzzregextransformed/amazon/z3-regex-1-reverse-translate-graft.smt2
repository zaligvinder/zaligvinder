(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "UUU") (re.++ re.allchar (str.to_re "---")))))))
(assert (str.in_re S (re.++ re.allchar (str.to_re "UUU---"))))
(check-sat)
