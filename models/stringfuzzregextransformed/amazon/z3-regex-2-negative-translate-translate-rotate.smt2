(set-logic QF_S)
(declare-const S String)
(assert (str.in.re S (re.++ (str.to.re "EEEiii") re.allchar)))
(assert (not (str.in.re S (re.++ re.allchar (re.++ (str.to.re "iii") (re.++ (str.to.re "EEE") re.allchar))))))
(check-sat)
