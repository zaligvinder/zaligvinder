(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ re.allchar (str.to_re "bbbaaa")))))
(assert (str.in_re S (re.++ (re.++ (re.++ re.allchar (str.to_re "aaa")) (str.to_re "bbb")) re.allchar)))
(check-sat)
