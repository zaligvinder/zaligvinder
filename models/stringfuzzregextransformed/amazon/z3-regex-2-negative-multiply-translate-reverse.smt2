(set-logic QF_S)
(declare-const S String)
(assert (str.in.re S (re.++ re.allchar (str.to.re "DDDDDDMMMMMM"))))
(assert (not (str.in.re S (re.++ re.allchar (re.++ (str.to.re "DDDDDD") (re.++ re.allchar (str.to.re "MMMMMM")))))))
(check-sat)
