(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "DDDDDDMMMMMM"))))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "DDDDDD") (re.++ re.allchar (str.to_re "MMMMMM")))))))
(check-sat)
