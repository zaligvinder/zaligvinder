(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ (str.to_re "xxxSSS") re.allchar))))
(assert (str.in_re S (re.++ (re.++ (re.++ (str.to_re "xxx") re.allchar) (str.to_re "SSS")) re.allchar)))
(check-sat)
