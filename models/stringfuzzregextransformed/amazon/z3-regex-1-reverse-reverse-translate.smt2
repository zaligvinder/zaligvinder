(set-logic QF_S)
(declare-const S String)
(assert (not (str.in.re S (re.++ (str.to.re "xxxSSS") re.allchar))))
(assert (str.in.re S (re.++ (re.++ (re.++ (str.to.re "xxx") re.allchar) (str.to.re "SSS")) re.allchar)))
(check-sat)
