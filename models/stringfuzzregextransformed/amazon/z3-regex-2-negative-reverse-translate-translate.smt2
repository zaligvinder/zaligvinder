(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "zzzEEE"))))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "zzz") (re.++ re.allchar (str.to_re "EEE")))))))
(check-sat)
