(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "aaabbb") re.allchar)))
(assert (not (str.in_re S (re.++ (str.to_re "aaa") (re.++ (re.++ re.allchar (str.to_re "bbb")) re.allchar)))))
(check-sat)
