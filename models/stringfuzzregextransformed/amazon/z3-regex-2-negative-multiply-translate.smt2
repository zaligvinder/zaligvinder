(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "MMMMMMDDDDDD") re.allchar)))
(assert (not (str.in_re S (re.++ (re.++ (re.++ (str.to_re "MMMMMM") re.allchar) (str.to_re "DDDDDD")) re.allchar))))
(check-sat)
