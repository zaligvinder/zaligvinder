(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "bbbaaa"))))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "bbb") (str.to_re "aaa"))))))
(check-sat)
