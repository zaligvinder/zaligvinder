(set-logic QF_S)
(declare-const S String)
(assert (not (str.in.re S (str.to.re "aaa"))))
(assert (str.in.re S (re.++ re.allchar (re.++ (re.++ (re.++ re.allchar (str.to.re "bbbaaa")) (str.to.re "bbb")) re.allchar))))
(check-sat)
