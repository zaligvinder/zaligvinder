(set-logic QF_S)
(declare-const S String)
(assert (str.in.re S (re.++ re.allchar (str.to.re "L1g.0CYg+)h"))))
(assert (not (str.in.re S (re.++ re.allchar (re.++ (str.to.re "S") (re.++ re.allchar (str.to.re "b%~h")))))))
(check-sat)
