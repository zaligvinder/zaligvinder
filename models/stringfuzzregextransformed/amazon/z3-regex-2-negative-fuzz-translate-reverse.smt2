(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ re.allchar (str.to_re "L1g.0CYg+)h"))))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "S") (re.++ re.allchar (str.to_re "b%~h")))))))
(check-sat)
