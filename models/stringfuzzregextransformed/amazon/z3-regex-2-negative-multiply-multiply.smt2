(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "aaaaaaaaaaaabbbbbbbbbbbb") re.allchar)))
(assert (not (str.in_re S (re.++ (re.++ (re.++ (str.to_re "aaaaaaaaaaaa") re.allchar) (str.to_re "bbbbbbbbbbbb")) re.allchar))))
(check-sat)
