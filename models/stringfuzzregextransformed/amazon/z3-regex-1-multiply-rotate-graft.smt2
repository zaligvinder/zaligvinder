(set-logic QF_S)
(declare-const S String)
(assert (not (str.in.re S (re.++ (str.to.re "aaaaaabbbbbb") re.allchar))))
(assert (str.in.re S (re.++ re.allchar (re.++ (str.to.re "bbbbbb") (str.to.re "aaaaaa")))))
(check-sat)
