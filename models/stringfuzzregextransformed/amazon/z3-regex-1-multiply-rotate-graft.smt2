(set-logic ALL)
(declare-const S String)
(assert (not (str.in_re S (re.++ (str.to_re "aaaaaabbbbbb") re.allchar))))
(assert (str.in_re S (re.++ re.allchar (re.++ (str.to_re "bbbbbb") (str.to_re "aaaaaa")))))
(check-sat)
