(set-logic ALL)
(declare-const S String)
(assert (str.in_re S (re.++ (str.to_re "ggggggwwwwww") re.allchar)))
(assert (not (str.in_re S (re.++ re.allchar (re.++ (str.to_re "wwwwww") (re.++ (str.to_re "gggggg") re.allchar))))))
(check-sat)
