(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "bb")) (str.to_re ""))))
(assert (str.in_re x (re.union (re.union (re.+ (str.to_re "")) (re.* (str.to_re "bb"))) (str.to_re "))"))))
(assert (= 2 (str.to_int x)))
(check-sat)
