(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "DD") (re.* (str.to_re "ww"))))))
(assert (= 4 (str.len x)))
(assert (not (= x "DDDD")))
(assert (not (= x "wwDD")))
(check-sat)
