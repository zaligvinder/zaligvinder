(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "<<CC"""">>"))))
(assert (str.in_re y (re.* (str.to_re "<<CC"""">>"))))
(assert (= (str.len x) 12))
(check-sat)
