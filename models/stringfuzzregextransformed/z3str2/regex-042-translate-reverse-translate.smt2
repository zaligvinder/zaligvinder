(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "P")) (str.to.re "Y"))))
(assert (str.in.re x (re.++ (re.++ (re.* (str.to.re "d")) (re.* (str.to.re "P"))) (str.to.re "Y"))))
(assert (= 3 (str.len x)))
(check-sat)
