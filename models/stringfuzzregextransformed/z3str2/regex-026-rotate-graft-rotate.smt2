(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "b") (re.* (re.* (str.to_re "a")))))))
(assert (str.in_re y (re.* (re.++ (str.to_re "b") (str.to_re "a")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
