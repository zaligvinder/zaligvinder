(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "rr~~}}--"))))
(assert (str.in_re x (re.* (str.to_re "rr~~}}--rr~~}}--"))))
(assert (> (str.len x) 40))
(assert (< (str.len x) 50))
(check-sat)
