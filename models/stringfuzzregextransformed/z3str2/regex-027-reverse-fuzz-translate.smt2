(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "P") (re.* (str.to_re ""))))))
(assert (= (str.to_int x) 1))
(assert (not (= x "]?pr0W?")))
(assert (not (= x "Yzlt9")))
(assert (not (= x "Y")))
(check-sat)
