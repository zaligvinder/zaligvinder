(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (re.* (str.to_re "b"))) (str.to_re "a")))))
(assert (str.in_re y (str.to_re "b")))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
