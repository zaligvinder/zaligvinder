(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.++ (str.to.re "I") (re.* (str.to.re "&"))) (re.* (str.to.re "&"))))))
(assert (str.in.re y (re.* (str.to.re "I"))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
