(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (re.* (re.union (re.+ (str.to_re "a")) (str.to_re "b")))) (str.to_re "b")))))
(assert (str.in_re y (str.to_re "a")))
(check-sat)
