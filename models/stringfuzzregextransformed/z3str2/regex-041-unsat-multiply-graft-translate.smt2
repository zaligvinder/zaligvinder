(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "LL") (re.* (str.to_re "zz")))))
(assert (str.in_re x (re.++ (str.to_re "ll") (re.* (re.* (str.to_re "ll"))))))
(assert (str.in_re x (re.++ (str.to_re "LL") (re.++ (re.* (str.to_re "zz")) (str.to_re "ll")))))
(check-sat)
