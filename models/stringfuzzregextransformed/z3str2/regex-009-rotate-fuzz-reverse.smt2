(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "dc|#""""a"))))
(assert (str.in_re x (re.* (str.to_re "dcaYg{a"))))
(assert (> (str.len x) 15))
(assert (< (str.len x) 22))
(check-sat)
