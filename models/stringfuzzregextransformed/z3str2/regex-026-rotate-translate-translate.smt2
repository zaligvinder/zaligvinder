(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "h")) (str.to.re """k=0Y""")))))
(assert (str.in.re y (re.* (re.++ (re.* (str.to.re "h")) (str.to.re """k=0Y""")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
