(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re """""")) (str.to_re "g")))))
(assert (str.in_re y (re.* (re.++ (str.to_re "g") (re.* (str.to_re """"""))))))
(check-sat)
