(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.* (str.to_re "5J")) (str.to_re "f")))))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "p)")) (str.to_re "dd")))))
(assert (= (str.to_int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 14))
(check-sat)
