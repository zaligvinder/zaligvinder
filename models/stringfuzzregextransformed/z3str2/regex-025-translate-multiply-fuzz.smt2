(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (re.* (str.to.re "5J")) (str.to.re "f")))))
(assert (str.in.re y (re.* (re.++ (re.* (str.to.re "p)")) (str.to.re "dd")))))
(assert (= (str.to.int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 14))
(check-sat)
