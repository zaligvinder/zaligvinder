(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "Z") (re.* (str.to_re "X")))))
(assert (str.in_re x (re.++ (str.to_re "Z") (re.++ (re.* (str.to_re "X")) (re.* (str.to_re "IUQI"))))))
(assert (= 3 (str.len x)))
(check-sat)
