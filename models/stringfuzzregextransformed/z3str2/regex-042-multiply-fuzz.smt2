(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "aa") (re.+ (str.to_re "O")))))
(assert (str.in_re x (re.++ (str.to_re "v") (re.union (re.* (str.to_re "b1")) (re.+ (str.to_re "c|"))))))
(assert (= 2 (str.len x)))
(check-sat)
