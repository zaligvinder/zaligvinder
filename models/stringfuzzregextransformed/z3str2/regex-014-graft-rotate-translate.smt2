(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.* (str.to_re "\\S")))))
(assert (str.in_re y (str.to_re "\\S")))
(assert (= 2 (str.len x)))
(assert (= (str.len y) 4))
(check-sat)
