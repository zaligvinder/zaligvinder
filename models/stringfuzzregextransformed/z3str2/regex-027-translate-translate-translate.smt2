(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "H")) (str.to_re "' '")))))
(assert (= (str.len x) 3))
(assert (not (= x "HH' '")))
(assert (not (= x "H' '' '")))
(assert (not (= x "' 'H' '")))
(check-sat)
