(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "") (re.+ (str.to_re "b"))))))
(assert (= (str.len x) 4))
(assert (not (= x "D")))
(assert (not (= x "<")))
(check-sat)
