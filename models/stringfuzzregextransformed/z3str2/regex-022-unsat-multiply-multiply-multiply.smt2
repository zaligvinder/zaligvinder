(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "aaaaaaaa")) (str.to_re "bbbbbbbb")))))
(assert (= (str.len x) 16))
(assert (not (= x "bbbbbbbbbbbbbbbb")))
(assert (not (= x "aaaaaaaabbbbbbbb")))
(check-sat)
