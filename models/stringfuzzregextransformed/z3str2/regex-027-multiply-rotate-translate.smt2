(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "//")) (str.to_re "JJ")))))
(assert (= (str.len x) 6))
(assert (not (= x "JJJJ//")))
(assert (not (= x "JJ////")))
(assert (not (= x "//JJ//")))
(check-sat)
