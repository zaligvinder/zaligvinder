(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.* (str.to_re "aabbccddaabbccdd")))))
(assert (str.in_re x (str.to_re "aabbccdd")))
(assert (> 40 (str.len x)))
(assert (< (str.len x) 50))
(check-sat)
