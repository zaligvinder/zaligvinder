(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "z") (re.* (str.to_re "s"))))))
(assert (str.in_re y (re.* (str.to_re "s"))))
(check-sat)
