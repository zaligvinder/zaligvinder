(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "b")) (str.to.re "_")))))
(assert (str.in.re y (re.+ (re.++ (re.+ (str.to.re "")) (str.to.re "")))))
(assert (not (= x y)))
(assert (= (str.to.int x) (str.to.int y)))
(check-sat)
