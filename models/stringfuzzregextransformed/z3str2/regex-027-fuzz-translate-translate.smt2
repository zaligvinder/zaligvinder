(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "m")) (str.to_re "")))))
(assert (= (str.len x) 0))
(assert (not (= x "mm1"";""..")))
(assert (not (= x "`y``y`3T""+]""")))
(assert (not (= x "m""+(""F")))
(check-sat)
