(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "aa") (re.* (str.to_re "bb")))))
(assert (str.in_re x (re.++ (re.union (re.+ (str.to_re "bb")) (re.+ (str.to_re "cc"))) (str.to_re ""))))
(assert (= 8 (str.len x)))
(check-sat)
