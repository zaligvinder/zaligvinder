(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "MM") (re.* (str.to_re "XX"))))))
(assert (= (str.len x) 4))
(assert (not (= x "MMMM")))
(assert (not (= x "MMXX")))
(check-sat)
