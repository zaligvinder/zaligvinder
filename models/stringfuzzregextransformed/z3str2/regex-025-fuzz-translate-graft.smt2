(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.* (str.to_re "?")) (str.to_re "=")))))
(assert (str.in_re y (re.+ (re.union (str.to_re "8") (re.* (str.to_re ""))))))
(assert (= (str.len x) 9))
(assert (not (= x y)))
(assert (= (str.to_int x) (str.to_int y)))
(check-sat)
