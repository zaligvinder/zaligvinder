(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (re.* (str.to.re "?")) (str.to.re "=")))))
(assert (str.in.re y (re.+ (re.union (str.to.re "8") (re.* (str.to.re ""))))))
(assert (= (str.len x) 9))
(assert (not (= x y)))
(assert (= (str.to.int x) (str.to.int y)))
(check-sat)
