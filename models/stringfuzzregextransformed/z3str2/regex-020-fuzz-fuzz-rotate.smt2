(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.* (str.to.re "b")) (str.to.re "}")))))
(assert (= (str.len x) 1))
(assert (not (= x "")))
(assert (not (= x "AG<WBF'\\n'3'\\x0c'")))
(assert (not (= x "cKLC]\\\\_vGYT!U)+")))
(check-sat)
