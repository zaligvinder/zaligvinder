(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "b")) (str.to_re "}")))))
(assert (= (str.len x) 1))
(assert (not (= x "")))
(assert (not (= x "AG<WBF'\\n'3'\\u{c}'")))
(assert (not (= x "cKLC]\\\\_vGYT!U)+")))
(check-sat)
