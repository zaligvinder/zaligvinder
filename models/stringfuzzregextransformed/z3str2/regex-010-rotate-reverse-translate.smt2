(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "f["))))
(assert (str.in_re x (re.* (str.to_re "f[f["))))
(assert (str.in_re x (re.* (str.to_re "H[f[f["))))
(check-sat)
