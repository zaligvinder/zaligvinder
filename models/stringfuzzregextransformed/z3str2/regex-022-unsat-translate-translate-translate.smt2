(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "K")) (str.to.re "b")))))
(assert (= (str.len x) 2))
(assert (not (= x "bb")))
(assert (not (= x "Kb")))
(check-sat)
