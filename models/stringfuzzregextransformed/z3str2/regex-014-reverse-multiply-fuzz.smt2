(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re ""))))
(assert (str.in_re y (re.* (str.to_re "bba"))))
(assert (= (str.len x) 2))
(assert (= (str.to_int y) 11))
(check-sat)
