(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "M") (re.* (str.to_re "J"))))))
(assert (= (str.len x) 3))
(assert (not (= x "JMM")))
(assert (not (= x "JJM")))
(assert (not (= x "JMJ")))
(check-sat)
