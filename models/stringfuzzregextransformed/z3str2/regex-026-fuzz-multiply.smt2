(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (re.+ (str.to_re "gg")) (str.to_re "bb")))))
(assert (str.in_re y (re.* (re.union (re.* (str.to_re "aa")) (str.to_re "LL")))))
(assert (not (= x y)))
(assert (= (str.to_int x) (str.to_int y)))
(check-sat)
