(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "aaaa")) (str.to_re "bbbb")))))
(assert (str.in_re y (re.* (re.union (re.+ (str.to_re "aaaa")) (str.to_re "bbbb")))))
(assert (= (str.len x) (str.to_int y)))
(assert (not (= x y)))
(assert (= (str.len x) 8))
(check-sat)
