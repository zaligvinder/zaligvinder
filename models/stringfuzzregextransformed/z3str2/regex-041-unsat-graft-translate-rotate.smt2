(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "O") (re.* (str.to_re "w")))))
(assert (str.in_re x (re.++ (str.to_re "e") (re.* (str.to_re "e")))))
(assert (str.in_re x (re.++ (re.* (str.to_re "O")) (re.++ (re.* (str.to_re "e")) (str.to_re "w")))))
(check-sat)
