(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "VA") (str.to_re "321")))))
(assert (= 5 (str.len x)))
(assert (not (= x "VA321")))
(check-sat)
