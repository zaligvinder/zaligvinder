(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "{{gg"))))
(assert (str.in_re x (re.* (str.to_re "{{gg{{gg"))))
(assert (str.in_re x (re.* (str.to_re "{{gg{{gg{{AA"))))
(assert (> (str.len x) 2))
(check-sat)
