(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (= (str.len x) 8))
(assert (= x y))
(assert (str.in_re y (re.+ (re.range "`" "a"))))
(assert (str.suffixof "" x))
(check-sat)
