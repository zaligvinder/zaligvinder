(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "aaaaaaaabbbbbbbb"))))
(assert (str.in_re y (re.* (str.to_re "aaaaaaaabbbbbbbb"))))
(assert (= (str.len x) 16))
(assert (= (str.len y) 32))
(check-sat)
