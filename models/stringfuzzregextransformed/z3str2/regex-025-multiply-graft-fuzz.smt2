(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.+ (re.* (str.to.re "A%"))) (str.to.re "3,")))))
(assert (str.in.re y (re.* (re.union (str.to.re "") (str.to.re "{' 'b")))))
(assert (= (str.len x) 14))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
