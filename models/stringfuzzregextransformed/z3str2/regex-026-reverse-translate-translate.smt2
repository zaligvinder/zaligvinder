(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "'") (re.* (str.to_re "H"))))))
(assert (str.in_re y (re.* (re.++ (str.to_re "'") (re.* (str.to_re "H"))))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
