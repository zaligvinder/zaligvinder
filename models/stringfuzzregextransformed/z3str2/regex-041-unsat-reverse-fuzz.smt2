(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "H")) (str.to_re "<"))))
(assert (str.in_re x (re.union (re.* (str.to_re "p")) (str.to_re "c"))))
(assert (str.in_re x (re.union (re.++ (re.+ (str.to_re "c")) (re.+ (str.to_re "b"))) (str.to_re "U"))))
(check-sat)
