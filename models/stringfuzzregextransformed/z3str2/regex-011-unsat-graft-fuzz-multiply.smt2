(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.* (str.to_re "aabbdd")))))
(assert (str.in_re y (str.to_re "aabbdd")))
(assert (= 24 (str.to_int x)))
(check-sat)
