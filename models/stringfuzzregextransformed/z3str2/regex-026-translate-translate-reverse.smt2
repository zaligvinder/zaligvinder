(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "HA0oZH") (re.* (str.to_re "y"))))))
(assert (str.in_re y (re.* (re.++ (str.to_re "HA0oZH") (re.* (str.to_re "y"))))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
