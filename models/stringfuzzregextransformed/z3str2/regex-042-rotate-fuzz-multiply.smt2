(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "99") (re.+ (str.to_re "tt")))))
(assert (str.in_re x (re.union (re.+ (re.* (str.to_re "cc"))) (re.++ (str.to_re "bb") (str.to_re "aa")))))
(assert (= 2 (str.len x)))
(check-sat)
