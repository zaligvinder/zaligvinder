(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "")) (str.to_re "t")))))
(assert (= (str.len x) 6))
(assert (not (= x "#,?a")))
(assert (not (= x "a.k*b")))
(assert (not (= x "B5'' ''b")))
(check-sat)
