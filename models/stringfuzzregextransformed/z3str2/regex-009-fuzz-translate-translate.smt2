(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "'\t'@i0%'\t'd"))))
(assert (str.in_re x (re.* (str.to_re "gesIf' 'rC/&di'\t'@i0%'\t'd"))))
(assert (> (str.to_int x) 17))
(assert (< (str.to_int x) 46))
(check-sat)
