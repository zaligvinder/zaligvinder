(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "x") (re.+ (str.to.re ""))))))
(assert (= (str.len x) 4))
(assert (not (= x ":")))
(assert (not (= x "G")))
(check-sat)
