(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (re.* (str.to_re "")) (str.to_re "*")))))
(assert (= (str.len x) 8))
(assert (not (= x "0")))
(assert (not (= x "")))
(check-sat)
