(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.* (str.to_re "")) (str.to_re "9")))))
(assert (= (str.len x) 3))
(assert (not (= x "'\u{c}'k&'c~")))
(assert (not (= x "&{{")))
(assert (not (= x "{")))
(check-sat)
