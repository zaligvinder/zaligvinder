(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.+ (str.to_re "1")) (str.to_re "")))))
(assert (= (str.len x) 7))
(assert (not (= x "Ma;|.")))
(assert (not (= x "Z::{..")))
(check-sat)
