(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.+ (str.to.re "1")) (str.to.re "")))))
(assert (= (str.len x) 7))
(assert (not (= x "Ma;|.")))
(assert (not (= x "Z::{..")))
(check-sat)
