(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.* (str.to_re "ab12")))))
(assert (str.in_re y (re.* (str.to_re "ab12"))))
(assert (= 8 4))
(assert (= (str.len y) (str.len x)))
(check-sat)
