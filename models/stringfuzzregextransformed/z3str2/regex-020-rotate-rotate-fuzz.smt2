(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (re.* (str.to_re "c")) (str.to_re "z")))))
(assert (= (str.to_int x) 2))
(assert (not (= x "b")))
(assert (not (= x "b")))
(assert (not (= x "FL")))
(check-sat)
