(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (= (str.to_int x) 2))
(assert (= x y))
(assert (str.in_re y (re.* (re.range "a" "j"))))
(assert (str.contains "" x))
(check-sat)
