(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.+ (str.to_re "")) (str.to_re "1")))))
(assert (= (str.len x) 7))
(assert (not (= x "`EP/b")))
(assert (not (= x "M""""abb")))
(check-sat)
