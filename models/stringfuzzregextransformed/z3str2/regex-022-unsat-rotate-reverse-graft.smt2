(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "a"))))
(assert (= 2 (str.len x)))
(assert (not (= x "bb")))
(assert (not (= x "ba")))
(check-sat)
