(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "[[")) (str.to_re "zz")))))
(assert (= (str.len x) 6))
(assert (not (= x "[[zzzz")))
(assert (not (= x "zz[[zz")))
(assert (not (= x "[[[[zz")))
(check-sat)
