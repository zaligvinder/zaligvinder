(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "ab"))))
(assert (str.in_re x (str.to_re "ababac")))
(assert (str.in_re x (re.* (re.* (str.to_re "abab")))))
(assert (> 1 (str.len x)))
(check-sat)
