(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "ppcc"))))
(assert (str.in_re y (re.* (str.to_re "ppcc"))))
(assert (= (str.len x) 4))
(assert (= (str.len y) 8))
(check-sat)
