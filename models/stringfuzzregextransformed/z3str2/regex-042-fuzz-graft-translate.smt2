(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (re.+ (str.to_re "-")) (re.* (str.to_re "c")))))
(assert (str.in_re x (re.++ (str.to_re "") (re.union (re.+ (str.to_re "c")) (str.to_re "%")))))
(assert (= (str.len x) 4))
(check-sat)
