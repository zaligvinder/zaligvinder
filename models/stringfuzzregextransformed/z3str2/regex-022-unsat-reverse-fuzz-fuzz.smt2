(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (str.to_re "b") (re.+ (str.to_re ""))))))
(assert (= (str.to_int x) 6))
(assert (not (= x "D")))
(assert (not (= x "2")))
(check-sat)
