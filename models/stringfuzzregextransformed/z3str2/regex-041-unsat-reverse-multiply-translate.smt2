(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "yy")) (str.to_re "!!"))))
(assert (str.in_re x (re.++ (re.* (str.to_re "++")) (str.to_re "++"))))
(assert (str.in_re x (re.++ (re.++ (re.* (str.to_re "++")) (re.* (str.to_re "yy"))) (str.to_re "!!"))))
(check-sat)
