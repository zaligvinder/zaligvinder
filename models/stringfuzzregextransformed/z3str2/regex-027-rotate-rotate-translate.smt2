(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "t")) (str.to_re "\\")))))
(assert (= (str.len x) 3))
(assert (not (= x "tt\\")))
(assert (not (= x "t\\\\")))
(assert (not (= x "\\t\\")))
(check-sat)
