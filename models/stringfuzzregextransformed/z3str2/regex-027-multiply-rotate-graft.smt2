(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "aa") (re.* (str.to_re "bb"))))))
(assert (= 6 (str.len x)))
(assert (not (= x "aaaabb")))
(assert (not (= x "aabbbb")))
(assert (not (= x "bbaabb")))
(check-sat)
