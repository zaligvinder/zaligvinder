(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "bbbb") (re.* (str.to_re "aaaa"))))))
(assert (= (str.len x) 8))
(assert (not (= x "bbbbbbbb")))
(assert (not (= x "bbbbaaaa")))
(check-sat)
