(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "ba")))
(assert (str.in_re x (re.* (str.to_re "baba"))))
(assert (str.in_re x (re.* (re.* (str.to_re "cababa")))))
(assert (> (str.len x) 1))
(check-sat)
