(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "ww") (re.* (str.to_re "VV")))))
(assert (str.in_re x (re.++ (str.to_re "ww") (re.++ (str.to_re "VV") (re.* (str.to_re "tt"))))))
(assert (= (str.len x) 6))
(check-sat)
