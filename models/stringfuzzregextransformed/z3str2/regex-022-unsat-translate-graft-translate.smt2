(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "k") (re.* (str.to_re "B"))))))
(assert (= 2 (str.len x)))
(assert (not (= x "kk")))
(assert (not (= x "Bk")))
(check-sat)
