(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "h") (re.* (str.to.re "D"))))))
(assert (= (str.len x) 2))
(assert (not (= x "hh")))
(assert (not (= x "hD")))
(check-sat)
