(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "/") (re.* (str.to_re "g"))))))
(assert (= 1 (str.len x)))
(assert (not (= x "t")))
(assert (not (= x "X'\t'")))
(check-sat)
