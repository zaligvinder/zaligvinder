(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "")) (str.to_re ""))))
(assert (str.in_re x (re.++ (re.union (re.* (str.to_re "5")) (str.to_re "o")) (re.* (str.to_re "3")))))
(assert (= (str.len x) 3))
(check-sat)
