(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (str.to.re "V")))
(assert (str.in.re y (re.* (re.union (re.* (re.union (str.to.re "b") (re.+ (str.to.re "N")))) (re.+ (str.to.re ""))))))
(assert (= (str.len x) 6))
(assert (not (= x y)))
(assert (= (str.to.int x) (str.len y)))
(check-sat)
