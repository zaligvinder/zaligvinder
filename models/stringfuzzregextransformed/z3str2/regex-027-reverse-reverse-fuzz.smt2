(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.+ (str.to_re "")) (str.to_re "b")))))
(assert (= (str.len x) 4))
(assert (not (= x "a")))
(assert (not (= x "b")))
(assert (not (= x "baO'")))
(check-sat)
