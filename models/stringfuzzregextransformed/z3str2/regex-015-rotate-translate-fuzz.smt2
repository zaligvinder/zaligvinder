(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "ui8"))))
(assert (str.in_re y (re.+ (re.* (str.to_re "1")))))
(assert (= (str.len x) 5))
(assert (= (str.len y) 12))
(check-sat)
