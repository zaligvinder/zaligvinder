(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (str.to.re "a")))
(assert (str.in.re y (re.* (re.* (re.++ (re.* (str.to.re "b")) (str.to.re "a"))))))
(check-sat)
