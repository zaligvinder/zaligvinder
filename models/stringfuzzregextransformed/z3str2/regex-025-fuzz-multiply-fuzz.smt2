(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.* (str.to.re "a'\t'")) (str.to.re "bb")))))
(assert (str.in.re y (re.* (re.union (re.+ (str.to.re "a")) (str.to.re "bZ")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 0))
(check-sat)
