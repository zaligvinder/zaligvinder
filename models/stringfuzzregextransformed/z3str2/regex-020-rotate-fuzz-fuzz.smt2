(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.+ (str.to_re "")) (str.to_re "0")))))
(assert (= (str.to_int x) 0))
(assert (not (= x "[")))
(assert (not (= x "b")))
(assert (not (= x "9")))
(check-sat)
