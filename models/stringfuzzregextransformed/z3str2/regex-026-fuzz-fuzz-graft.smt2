(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "b") (re.* (str.to.re ""))))))
(assert (str.in.re y (re.* (re.union (re.* (str.to.re "")) (str.to.re "m")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.to.int y)))
(check-sat)
