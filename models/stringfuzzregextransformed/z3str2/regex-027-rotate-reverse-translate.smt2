(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "X") (re.* (str.to.re ">"))))))
(assert (= (str.len x) 3))
(assert (not (= x ">XX")))
(assert (not (= x ">>X")))
(assert (not (= x ">X>")))
(check-sat)
