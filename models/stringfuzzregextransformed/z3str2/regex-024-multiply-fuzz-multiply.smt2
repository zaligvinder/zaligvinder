(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.+ (str.to_re "aa")) (str.to_re "bbbb")))))
(assert (str.in_re y (re.* (re.++ (re.+ (str.to_re "EEpp")) (str.to_re "##")))))
(check-sat)
