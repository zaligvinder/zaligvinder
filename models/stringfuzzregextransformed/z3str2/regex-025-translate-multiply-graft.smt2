(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "[[")))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "[[")) (str.to_re "dd")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= 14 (str.len x)))
(check-sat)
