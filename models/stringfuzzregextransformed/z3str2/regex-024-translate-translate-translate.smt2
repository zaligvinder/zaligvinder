(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "##")) (str.to_re "Y")))))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "##")) (str.to_re "Y")))))
(check-sat)
