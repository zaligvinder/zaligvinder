(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "1' '_' '") (re.+ (str.to_re "DD")))))
(assert (str.in_re x (re.++ (str.to_re "q") (re.++ (re.+ (str.to_re "' '_' 'R' '_' '")) (re.* (str.to_re "7"))))))
(assert (= 0 (str.len x)))
(check-sat)
