(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "CC")) (str.to.re "YY")))))
(assert (= (str.len x) 6))
(assert (not (= x "CCCCYY")))
(assert (not (= x "CCYYYY")))
(assert (not (= x "YYCCYY")))
(check-sat)
