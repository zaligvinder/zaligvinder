(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "aabb"))))
(assert (str.in_re y (re.+ (str.to_re "aa"))))
(assert (= (str.to_int x) 6))
(assert (= (str.to_int y) 14))
(check-sat)
