(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "&") (str.to_re "{")))))
(assert (= 3 (str.len x)))
(assert (not (= x "&&{")))
(assert (not (= x "&{{")))
(assert (not (= x "{&{")))
(check-sat)
