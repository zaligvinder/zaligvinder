(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (re.+ (str.to_re "g")) (str.to_re ""))))
(assert (str.in_re x (re.union (re.++ (re.* (str.to_re "")) (re.+ (str.to_re "b"))) (str.to_re "e"))))
(assert (= 1 (str.len x)))
(check-sat)
