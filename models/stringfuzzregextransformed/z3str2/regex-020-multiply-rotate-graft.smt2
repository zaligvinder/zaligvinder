(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "bb") (str.to_re "aa")))))
(assert (= 6 (str.len x)))
(assert (not (= x "aabbbb")))
(assert (not (= x "bbaabb")))
(assert (not (= x "aaaabb")))
(check-sat)
