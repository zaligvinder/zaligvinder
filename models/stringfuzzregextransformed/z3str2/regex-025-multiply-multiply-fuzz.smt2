(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.* (str.to.re "")) (str.to.re "bbZK")))))
(assert (str.in.re y (re.* (re.++ (re.+ (str.to.re "0AyaaGYOq")) (str.to.re "sEbs&U/ojc")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 54))
(check-sat)
