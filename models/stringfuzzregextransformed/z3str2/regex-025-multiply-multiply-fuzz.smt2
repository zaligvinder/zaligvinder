(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "")) (str.to_re "bbZK")))))
(assert (str.in_re y (re.* (re.++ (re.+ (str.to_re "0AyaaGYOq")) (str.to_re "sEbs&U/ojc")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.to_int x) 54))
(check-sat)
