(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "a") (re.* (str.to_re "b"))))))
(assert (= (str.len x) 3))
(assert (not (= x "bba")))
(assert (not (= x "bab")))
(assert (not (= x "baa")))
(check-sat)
