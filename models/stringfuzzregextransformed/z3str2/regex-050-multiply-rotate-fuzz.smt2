(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (= (str.len x) 4))
(assert (= x y))
(assert (str.in_re y (re.+ (re.range "a" "c"))))
(assert (str.contains "1" x))
(check-sat)
