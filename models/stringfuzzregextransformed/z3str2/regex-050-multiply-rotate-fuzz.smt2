(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (= (str.len x) 4))
(assert (= x y))
(assert (str.in.re y (re.+ (re.range "a" "c"))))
(assert (str.contains "1" x))
(check-sat)
