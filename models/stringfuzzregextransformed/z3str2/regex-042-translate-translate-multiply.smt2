(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "jj") (re.* (str.to_re "ee")))))
(assert (str.in_re x (re.++ (str.to_re "jj") (re.++ (re.* (str.to_re "ee")) (re.* (str.to_re "''\\\\tt''"))))))
(assert (= 6 (str.len x)))
(check-sat)
