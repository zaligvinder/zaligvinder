(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.+ (str.to_re "11")) (str.to_re "")))))
(assert (= (str.len x) 14))
(assert (not (= x "``EEPP//bb")))
(assert (not (= x "MM""""""""aabbbb")))
(check-sat)
