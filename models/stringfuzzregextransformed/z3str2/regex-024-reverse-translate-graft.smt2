(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "+") (str.to_re "+")))))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "t")) (re.* (str.to_re "t"))))))
(check-sat)
