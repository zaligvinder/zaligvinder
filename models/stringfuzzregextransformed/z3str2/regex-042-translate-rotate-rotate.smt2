(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "*") (re.* (str.to.re "X")))))
(assert (str.in.re x (re.++ (str.to.re "$") (re.* (re.* (re.++ (str.to.re "X") (str.to.re "*")))))))
(assert (= 3 (str.len x)))
(check-sat)
