(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "GG")) (re.++ (re.* (str.to_re "GG")) (str.to_re "||"))))))
(assert (str.in_re y (re.* (str.to_re "||"))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
