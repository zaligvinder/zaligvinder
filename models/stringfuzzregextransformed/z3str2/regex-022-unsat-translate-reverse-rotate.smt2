(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "D") (re.* (str.to_re "t"))))))
(assert (= (str.len x) 2))
(assert (not (= x "tt")))
(assert (not (= x "tD")))
(check-sat)
