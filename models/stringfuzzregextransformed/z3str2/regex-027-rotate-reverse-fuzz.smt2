(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "") (re.+ (str.to_re "F"))))))
(assert (= (str.to_int x) 2))
(assert (not (= x "ej")))
(assert (not (= x "(tZA")))
(assert (not (= x "ba")))
(check-sat)
