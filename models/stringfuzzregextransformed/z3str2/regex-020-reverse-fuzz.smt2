(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "?") (re.* (str.to_re "E"))))))
(assert (= (str.to_int x) 0))
(assert (not (= x "a")))
(assert (not (= x "")))
(assert (not (= x "z[a")))
(check-sat)
