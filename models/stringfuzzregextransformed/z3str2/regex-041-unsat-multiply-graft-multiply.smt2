(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "aaaa") (re.* (str.to_re "bbbb")))))
(assert (str.in_re x (re.++ (str.to_re "cccc") (re.* (re.* (str.to_re "cccc"))))))
(assert (str.in_re x (re.++ (str.to_re "aaaa") (re.++ (re.* (str.to_re "bbbb")) (str.to_re "cccc")))))
(check-sat)
