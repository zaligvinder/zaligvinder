(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "") (re.+ (str.to_re "q"))))))
(assert (str.in_re y (re.+ (re.union (str.to_re "y") (re.* (str.to_re "z"))))))
(check-sat)
