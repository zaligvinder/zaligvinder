(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "\\c!"))))
(assert (str.in_re y (re.+ (str.to_re "CF"))))
(assert (= (str.len x) 2))
(assert (= (str.to_int y) 8))
(check-sat)
