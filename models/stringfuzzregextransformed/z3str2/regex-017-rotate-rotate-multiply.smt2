(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "112233") (re.union (str.to_re "AABB") (str.to_re "aabbccdd"))))))
(assert (= 10 (str.len x)))
(check-sat)
