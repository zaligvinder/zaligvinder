(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (str.to_re "B") (str.to_re "123")))))
(assert (= 3 (str.to_int x)))
(assert (not (= x "3A")))
(check-sat)
