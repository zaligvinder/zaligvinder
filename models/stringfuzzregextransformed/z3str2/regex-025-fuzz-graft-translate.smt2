(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "U")) (str.to_re "w")))))
(assert (str.in_re y (str.to_re "w")))
(assert (= (str.len x) 2))
(assert (not (= x y)))
(assert (= (str.len x) (str.to_int y)))
(check-sat)
