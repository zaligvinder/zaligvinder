(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.* (str.to.re "U")) (str.to.re "w")))))
(assert (str.in.re y (str.to.re "w")))
(assert (= (str.len x) 2))
(assert (not (= x y)))
(assert (= (str.len x) (str.to.int y)))
(check-sat)
