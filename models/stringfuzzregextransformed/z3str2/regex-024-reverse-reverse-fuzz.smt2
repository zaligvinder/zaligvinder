(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.+ (str.to_re "")) (str.to_re "")))))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "j")) (str.to_re "b")))))
(check-sat)
