(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "bb")) (str.to_re "wa"))))
(assert (str.in_re x (re.union (str.to_re "c") (re.+ (str.to_re "KZR")))))
(assert (str.in_re x (re.++ (re.++ (re.* (str.to_re "@hc")) (re.* (str.to_re "'c0x\\'4"))) (str.to_re "a"))))
(check-sat)
