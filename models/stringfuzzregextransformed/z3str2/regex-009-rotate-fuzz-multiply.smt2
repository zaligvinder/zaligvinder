(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "aa""""""""##||ccdd"))))
(assert (str.in_re x (re.* (str.to_re "aa{{ggYYaaccdd"))))
(assert (> (str.len x) 30))
(assert (< (str.len x) 44))
(check-sat)
