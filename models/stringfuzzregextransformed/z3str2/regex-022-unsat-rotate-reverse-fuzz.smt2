(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (str.to_re "a") (re.* (str.to_re ""))))))
(assert (= (str.to_int x) 3))
(assert (not (= x "bb")))
(assert (not (= x "ba")))
(check-sat)
