(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "FF") (re.* (str.to_re "aa")))))
(assert (str.in_re x (re.union (str.to_re "cc") (re.+ (str.to_re "")))))
(assert (str.in_re x (re.union (re.* (re.* (str.to_re "cc"))) (re.union (str.to_re "") (str.to_re "aa")))))
(check-sat)
