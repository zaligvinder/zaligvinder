(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "}")))
(assert (= 3 (str.len x)))
(assert (not (= x "'\\u{c}'}}")))
(assert (not (= x "}'\\u{c}'}")))
(assert (not (= x "'\\u{c}''\\u{c}'}")))
(check-sat)
