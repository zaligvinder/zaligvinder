(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "tt") (re.* (str.to.re "DD"))))))
(assert (= (str.len x) 4))
(assert (not (= x "tttt")))
(assert (not (= x "ttDD")))
(check-sat)
