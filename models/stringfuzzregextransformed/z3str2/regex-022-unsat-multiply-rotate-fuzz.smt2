(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.+ (str.to_re "`")) (str.to_re "a'")))))
(assert (= (str.len x) 1))
(assert (not (= x "bA[b")))
(assert (not (= x "%?d""b@")))
(check-sat)
