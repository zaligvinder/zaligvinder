(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (str.to.re "b")))
(assert (str.in.re y (re.+ (re.union (re.+ (str.to.re "b")) (str.to.re "")))))
(assert (= (str.to.int x) (str.to.int y)))
(assert (not (= x y)))
(assert (= 13 (str.len x)))
(check-sat)
