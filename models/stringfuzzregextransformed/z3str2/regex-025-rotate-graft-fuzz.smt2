(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "b")))
(assert (str.in_re y (re.+ (re.union (re.+ (str.to_re "b")) (str.to_re "")))))
(assert (= (str.to_int x) (str.to_int y)))
(assert (not (= x y)))
(assert (= 13 (str.len x)))
(check-sat)
