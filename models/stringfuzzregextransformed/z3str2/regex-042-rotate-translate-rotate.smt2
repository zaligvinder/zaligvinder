(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "m") (re.* (str.to_re "'\\u{c}'")))))
(assert (str.in_re x (re.++ (str.to_re "x") (re.* (re.* (re.++ (str.to_re "'\\u{c}'") (str.to_re "m")))))))
(assert (= 3 (str.len x)))
(check-sat)
