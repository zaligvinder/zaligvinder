(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (str.to.re "d") (re.+ (str.to.re "["))))))
(assert (str.in.re y (re.+ (re.union (str.to.re "d") (re.* (str.to.re "["))))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 8))
(check-sat)
