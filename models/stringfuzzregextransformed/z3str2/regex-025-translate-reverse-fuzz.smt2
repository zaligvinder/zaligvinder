(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "d") (re.+ (str.to_re "["))))))
(assert (str.in_re y (re.+ (re.union (str.to_re "d") (re.* (str.to_re "["))))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 8))
(check-sat)
