(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "D")) (str.to_re "t")))))
(assert (= 2 (str.len x)))
(assert (not (= x "tt")))
(assert (not (= x "tD")))
(check-sat)
