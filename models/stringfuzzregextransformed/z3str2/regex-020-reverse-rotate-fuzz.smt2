(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "") (re.* (str.to_re "'\n'"))))))
(assert (= (str.len x) 2))
(assert (not (= x "AEx")))
(assert (not (= x "!Hb`")))
(assert (not (= x "ba")))
(check-sat)
