(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "b") (re.* (str.to_re "a"))))))
(assert (= 3 (str.len x)))
(assert (not (= x "abb")))
(assert (not (= x "bab")))
(assert (not (= x "aab")))
(check-sat)
