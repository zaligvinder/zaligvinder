(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "ab}l"))))
(assert (str.in_re y (re.* (str.to_re "0Kbb"))))
(assert (= (str.to_int x) 4))
(assert (= (str.len y) 15))
(check-sat)
