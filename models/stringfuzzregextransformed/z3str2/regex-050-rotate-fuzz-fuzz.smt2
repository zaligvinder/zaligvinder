(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (= (str.len x) 1))
(assert (= x y))
(assert (str.in.re y (re.* (re.range "T" "_"))))
(assert (str.suffixof "" x))
(check-sat)
