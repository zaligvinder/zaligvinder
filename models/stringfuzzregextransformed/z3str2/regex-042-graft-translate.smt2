(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "w") (re.* (str.to.re "V")))))
(assert (str.in.re x (re.++ (str.to.re "w") (re.++ (str.to.re "V") (re.* (str.to.re "t"))))))
(assert (= (str.len x) 3))
(check-sat)
