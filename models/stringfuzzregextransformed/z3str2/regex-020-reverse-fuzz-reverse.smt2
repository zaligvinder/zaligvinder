(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "r") (re.+ (str.to_re ""))))))
(assert (= (str.len x) 4))
(assert (not (= x "AONb")))
(assert (not (= x "a_`k")))
(assert (not (= x "a/")))
(check-sat)
