(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "A")) (str.to_re "s")))))
(assert (= (str.to_int x) 3))
(assert (not (= x "AA")))
(assert (not (= x "b")))
(check-sat)
