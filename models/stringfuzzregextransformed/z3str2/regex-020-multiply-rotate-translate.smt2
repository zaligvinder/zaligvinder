(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "FF")) (str.to_re "xx")))))
(assert (= (str.len x) 6))
(assert (not (= x "xxFFFF")))
(assert (not (= x "FFxxFF")))
(assert (not (= x "xxxxFF")))
(check-sat)
