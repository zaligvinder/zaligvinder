(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "ww")) (str.to_re "DD")))))
(assert (= (str.len x) 4))
(assert (not (= x "DDDD")))
(assert (not (= x "wwDD")))
(check-sat)
