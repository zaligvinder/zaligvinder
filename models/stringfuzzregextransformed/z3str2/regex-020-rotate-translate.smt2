(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "}")) (str.to_re "'\u{c}'")))))
(assert (= (str.len x) 3))
(assert (not (= x "'\u{c}'}}")))
(assert (not (= x "}'\u{c}'}")))
(assert (not (= x "'\u{c}''\u{c}'}")))
(check-sat)
