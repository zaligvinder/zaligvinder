(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "ab12")))
(assert (str.in_re y (re.* (re.* (re.* (str.to_re "ab12"))))))
(assert (= 4 8))
(assert (= (str.len y) (str.len x)))
(check-sat)
