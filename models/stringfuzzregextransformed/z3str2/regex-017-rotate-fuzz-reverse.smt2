(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (re.++ (str.to_re "BA") (str.to_re "32")) (str.to_re "'jo~[!b4")))))
(assert (= 3 (str.len x)))
(check-sat)
