(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "O") (re.* (str.to_re "w")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "j"))) (re.++ (str.to_re "O") (str.to_re "w")))))
(assert (= 3 (str.len x)))
(check-sat)
