(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "PP")) (str.to_re "UU")))))
(assert (str.in_re y (re.* (re.union (re.+ (str.to_re "PP")) (str.to_re "UU")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.to_int y)))
(check-sat)
