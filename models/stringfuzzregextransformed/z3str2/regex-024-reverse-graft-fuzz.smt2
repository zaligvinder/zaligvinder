(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "b") (re.* (str.to_re ""))))))
(assert (str.in_re y (re.+ (re.union (re.+ (str.to_re "a")) (str.to_re "")))))
(check-sat)
