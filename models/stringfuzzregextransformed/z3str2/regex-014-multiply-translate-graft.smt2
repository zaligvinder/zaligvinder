(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "ppcc")))
(assert (str.in_re y (re.* (re.* (str.to_re "ppcc")))))
(assert (= (str.len x) (str.len y)))
(assert (= 4 8))
(check-sat)
