(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "1q'\t'a"))))
(assert (str.in_re y (re.* (re.* (str.to_re "a")))))
(assert (= (str.to_int x) 2))
(assert (= (str.to_int y) 5))
(check-sat)
