(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (str.to.re "1q'\t'a"))))
(assert (str.in.re y (re.* (re.* (str.to.re "a")))))
(assert (= (str.to.int x) 2))
(assert (= (str.to.int y) 5))
(check-sat)
