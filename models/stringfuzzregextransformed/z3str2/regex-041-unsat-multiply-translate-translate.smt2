(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "ii") (re.* (str.to_re "bb")))))
(assert (str.in_re x (re.++ (str.to_re "OO") (re.* (str.to_re "OO")))))
(assert (str.in_re x (re.++ (str.to_re "ii") (re.++ (re.* (str.to_re "bb")) (re.* (str.to_re "OO"))))))
(check-sat)
