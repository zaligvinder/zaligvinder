(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "O")) (str.to_re "?")))))
(assert (= (str.len x) 3))
(assert (not (= x "OO?")))
(assert (not (= x "O??")))
(assert (not (= x "?O?")))
(check-sat)
