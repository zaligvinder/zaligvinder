(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (re.* (str.to.re "")) (str.to.re "M")))))
(assert (= (str.len x) 0))
(assert (not (= x "ay/4b")))
(assert (not (= x "ab")))
(assert (not (= x "q")))
(check-sat)
