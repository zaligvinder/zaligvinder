(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.* (str.to_re "")) (str.to_re "M")))))
(assert (= (str.len x) 0))
(assert (not (= x "ay/4b")))
(assert (not (= x "ab")))
(assert (not (= x "q")))
(check-sat)
