(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (re.+ (str.to_re "!")) (str.to_re "Z")))))
(assert (str.in_re y (re.+ (re.++ (re.+ (str.to_re "|")) (str.to_re "G")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.to_int y)))
(check-sat)
