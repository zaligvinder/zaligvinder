(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "''") (re.* (str.to_re "k")))))
(assert (str.in_re x (re.++ (str.to_re "Y") (re.* (str.to_re "Y")))))
(assert (str.in_re x (re.++ (str.to_re "''") (re.++ (re.* (re.* (str.to_re "Y"))) (str.to_re "k")))))
(check-sat)
