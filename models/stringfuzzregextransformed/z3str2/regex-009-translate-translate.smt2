(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "$|+g"))))
(assert (str.in_re x (re.* (str.to_re "$|+g$|+g"))))
(assert (> (str.len x) 20))
(assert (< (str.len x) 25))
(check-sat)
