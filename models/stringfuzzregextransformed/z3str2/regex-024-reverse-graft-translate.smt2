(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "L") (re.* (str.to_re "d"))))))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "d")) (str.to_re "L")))))
(check-sat)
