(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "b") (re.* (str.to_re "a"))))))
(assert (= 2 (str.len x)))
(assert (not (= x "bb")))
(assert (not (= x "ab")))
(check-sat)
