(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "tt") (re.* (str.to.re "TT"))))))
(assert (= (str.len x) 6))
(assert (not (= x "ttttTT")))
(assert (not (= x "ttTTtt")))
(assert (not (= x "ttTTTT")))
(check-sat)
