(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "tt") (re.* (str.to_re "TT"))))))
(assert (= (str.len x) 6))
(assert (not (= x "ttttTT")))
(assert (not (= x "ttTTtt")))
(assert (not (= x "ttTTTT")))
(check-sat)
