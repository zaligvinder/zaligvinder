(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "mm")) (str.to_re "cc")))))
(assert (str.in_re y (re.* (str.to_re "mm"))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
