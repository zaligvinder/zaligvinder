(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "w") (re.* (str.to_re "^"))))))
(assert (= (str.len x) 2))
(assert (not (= x "ww")))
(assert (not (= x "w^")))
(check-sat)
