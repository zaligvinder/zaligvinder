(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (str.to_re "") (re.+ (str.to_re ""))))))
(assert (= 0 (str.len x)))
(assert (not (= x "bb")))
(assert (not (= x "~P")))
(check-sat)
