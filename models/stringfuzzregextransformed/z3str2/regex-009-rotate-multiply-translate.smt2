(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "vvooPP__"))))
(assert (str.in_re x (re.* (str.to_re "vvooPP__vvooPP__"))))
(assert (> (str.len x) 40))
(assert (< (str.len x) 50))
(check-sat)
