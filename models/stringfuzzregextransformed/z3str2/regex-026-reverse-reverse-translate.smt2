(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "@")) (str.to_re "B")))))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "@")) (str.to_re "B")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
