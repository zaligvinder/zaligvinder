(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "abcdabcd")))
(assert (str.in_re x (re.* (re.* (str.to_re "abcd")))))
(assert (> 25 20))
(assert (< (str.len x) (str.len x)))
(check-sat)
