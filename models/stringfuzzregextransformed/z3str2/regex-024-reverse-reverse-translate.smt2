(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "B")) (str.to.re "J")))))
(assert (str.in.re y (re.* (re.++ (re.* (str.to.re "B")) (str.to.re "J")))))
(check-sat)
