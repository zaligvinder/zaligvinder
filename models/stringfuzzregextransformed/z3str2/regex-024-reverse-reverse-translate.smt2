(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "B")) (str.to_re "J")))))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "B")) (str.to_re "J")))))
(check-sat)
