(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "O") (re.+ (str.to_re ".'I")))))
(assert (str.in_re x (re.++ (str.to_re "vv") (re.* (str.to_re "vv")))))
(assert (str.in_re x (re.++ (str.to_re "O") (re.++ (re.+ (str.to_re "ZS")) (re.* (str.to_re "v"))))))
(check-sat)
