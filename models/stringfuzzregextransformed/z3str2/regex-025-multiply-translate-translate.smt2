(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "ZZ")) (str.to_re "ee")))))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "ZZ")) (str.to_re "ee")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 14))
(check-sat)
