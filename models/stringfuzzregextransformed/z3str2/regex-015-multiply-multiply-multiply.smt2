(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "aaaaaaaabbbbbbbb1111111122222222"))))
(assert (str.in_re y (re.* (re.* (str.to_re "aaaaaaaabbbbbbbb1111111122222222")))))
(assert (= (str.len x) 32))
(assert (= (str.len y) 64))
(check-sat)
