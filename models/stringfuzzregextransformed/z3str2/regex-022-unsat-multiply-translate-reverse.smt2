(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "DD") (re.* (str.to_re "ww"))))))
(assert (= (str.len x) 4))
(assert (not (= x "DDDD")))
(assert (not (= x "DDww")))
(check-sat)
