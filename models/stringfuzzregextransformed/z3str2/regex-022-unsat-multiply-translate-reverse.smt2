(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "DD") (re.* (str.to.re "ww"))))))
(assert (= (str.len x) 4))
(assert (not (= x "DDDD")))
(assert (not (= x "DDww")))
(check-sat)
