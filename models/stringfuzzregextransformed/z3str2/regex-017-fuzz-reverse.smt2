(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "BA") (re.union (str.to_re "ea") (str.to_re "wU$"))))))
(assert (= 10 (str.len x)))
(check-sat)
