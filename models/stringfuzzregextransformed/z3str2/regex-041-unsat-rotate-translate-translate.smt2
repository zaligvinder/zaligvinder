(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "h") (re.* (str.to_re "G")))))
(assert (str.in_re x (re.++ (str.to_re "[") (re.* (str.to_re "[")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "["))) (re.++ (str.to_re "h") (str.to_re "G")))))
(check-sat)
