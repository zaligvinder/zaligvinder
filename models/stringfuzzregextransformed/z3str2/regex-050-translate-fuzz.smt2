(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (= (str.len x) 4))
(assert (= x y))
(assert (str.in_re y (re.* (re.range "b" "z"))))
(assert (str.suffixof "" x))
(check-sat)
