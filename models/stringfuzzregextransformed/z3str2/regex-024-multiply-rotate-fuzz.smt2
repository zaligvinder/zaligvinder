(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.+ (str.to_re "E")) (str.to_re "ma")))))
(assert (str.in_re y (re.* (re.union (re.+ (str.to_re "S'\t'b")) (str.to_re "aa")))))
(check-sat)
