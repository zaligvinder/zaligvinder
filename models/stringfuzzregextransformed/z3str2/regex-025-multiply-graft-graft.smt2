(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (str.to.re "bb")))
(assert (str.in.re y (re.* (re.++ (str.to.re "aa") (re.* (re.++ (re.* (re.* (str.to.re "aa"))) (str.to.re "bb")))))))
(assert (= (str.len x) (str.len x)))
(assert (not (= x y)))
(assert (= 14 (str.len y)))
(check-sat)
