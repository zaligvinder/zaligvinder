(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "bb")))
(assert (str.in_re y (re.* (re.++ (str.to_re "aa") (re.* (re.++ (re.* (re.* (str.to_re "aa"))) (str.to_re "bb")))))))
(assert (= (str.len x) (str.len x)))
(assert (not (= x y)))
(assert (= 14 (str.len y)))
(check-sat)
