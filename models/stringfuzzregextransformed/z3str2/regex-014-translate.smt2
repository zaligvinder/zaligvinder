(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "*C"))))
(assert (str.in_re y (re.* (str.to_re "*C"))))
(assert (= (str.len x) 2))
(assert (= (str.len y) 4))
(check-sat)
