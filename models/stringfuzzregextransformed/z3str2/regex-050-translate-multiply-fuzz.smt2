(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (= (str.len x) 5))
(assert (= x y))
(assert (str.in_re y (re.* (re.range "a" "s"))))
(assert (str.suffixof "11" x))
(check-sat)
