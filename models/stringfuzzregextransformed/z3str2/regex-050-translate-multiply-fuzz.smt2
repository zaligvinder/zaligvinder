(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (= (str.len x) 5))
(assert (= x y))
(assert (str.in.re y (re.* (re.range "a" "s"))))
(assert (str.suffixof "11" x))
(check-sat)
