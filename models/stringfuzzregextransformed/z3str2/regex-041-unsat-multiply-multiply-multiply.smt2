(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "aaaaaaaa") (re.* (str.to_re "bbbbbbbb")))))
(assert (str.in_re x (re.++ (str.to_re "cccccccc") (re.* (str.to_re "cccccccc")))))
(assert (str.in_re x (re.++ (str.to_re "aaaaaaaa") (re.++ (re.* (str.to_re "bbbbbbbb")) (re.* (str.to_re "cccccccc"))))))
(check-sat)
