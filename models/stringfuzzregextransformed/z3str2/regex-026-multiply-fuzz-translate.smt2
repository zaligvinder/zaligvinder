(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.+ (str.to_re "@")) (str.to_re "N")))))
(assert (str.in_re y (re.+ (re.union (re.+ (str.to_re "V*")) (str.to_re "shq0fs")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
