(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "b")) (str.to_re "L")))))
(assert (= (str.to_int x) 6))
(assert (not (= x "vaOb")))
(assert (not (= x "b")))
(assert (not (= x "_7I")))
(check-sat)
