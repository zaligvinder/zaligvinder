(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "OI") (re.+ (str.to_re "0+a"))))))
(assert (str.in_re y (re.+ (re.++ (str.to_re "bb") (re.* (str.to_re "^:a"))))))
(check-sat)
