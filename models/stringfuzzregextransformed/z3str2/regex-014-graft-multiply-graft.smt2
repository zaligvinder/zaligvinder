(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "aabb"))))
(assert (str.in_re y (re.* (str.to_re "aabb"))))
(assert (= 4 (str.len x)))
(assert (= 8 (str.len y)))
(check-sat)
