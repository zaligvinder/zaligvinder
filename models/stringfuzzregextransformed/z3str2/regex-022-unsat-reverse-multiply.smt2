(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "bb") (re.* (str.to_re "aa"))))))
(assert (= (str.len x) 4))
(assert (not (= x "bbbb")))
(assert (not (= x "bbaa")))
(check-sat)
