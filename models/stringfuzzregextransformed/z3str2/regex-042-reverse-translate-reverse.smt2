(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "d") (re.* (str.to_re "h")))))
(assert (str.in_re x (re.++ (str.to_re "d") (re.++ (re.* (str.to_re "h")) (re.* (str.to_re "?"))))))
(assert (= 3 (str.len x)))
(check-sat)
