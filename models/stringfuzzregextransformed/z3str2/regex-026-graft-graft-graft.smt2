(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "b")))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "a")) (re.* (str.to_re "a"))))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
