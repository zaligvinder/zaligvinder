(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.* (str.to_re "aabb")))))
(assert (str.in_re y (str.to_re "aabb")))
(assert (= (str.len x) 4))
(assert (= 8 (str.len y)))
(check-sat)
