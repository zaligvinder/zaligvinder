(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "a") (re.* (str.to_re "F")))))
(assert (str.in_re x (re.union (str.to_re "") (re.+ (str.to_re "c")))))
(assert (str.in_re x (re.union (str.to_re "c") (re.* (re.* (re.union (str.to_re "") (str.to_re "a")))))))
(check-sat)
