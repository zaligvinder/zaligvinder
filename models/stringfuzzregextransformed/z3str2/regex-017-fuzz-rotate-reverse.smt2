(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "dcbP!$") (re.++ (str.to_re "BA") (str.to_re "{'whAh"))))))
(assert (= 1 (str.to_int x)))
(check-sat)
