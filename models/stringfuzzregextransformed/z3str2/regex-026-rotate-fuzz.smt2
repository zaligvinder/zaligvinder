(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.* (str.to_re "6")) (str.to_re "U")))))
(assert (str.in_re y (re.* (re.union (re.+ (str.to_re "'\u{b}'")) (str.to_re "a")))))
(assert (not (= x y)))
(assert (= (str.to_int x) (str.len y)))
(check-sat)
