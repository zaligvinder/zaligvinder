(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "aaaa")) (str.to_re "bbbb")))))
(assert (str.in_re y (str.to_re "bbbb")))
(assert (= (str.len x) 28))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
