(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "HH") (re.* (str.to_re "JJ")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "!!"))) (re.++ (str.to_re "HH") (str.to_re "JJ")))))
(assert (= 6 (str.len x)))
(check-sat)
