(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "bb") (str.to_re "aa")))))
(assert (= 6 (str.len x)))
(assert (not (= x "bbaaaa")))
(assert (not (= x "bbbbaa")))
(assert (not (= x "bbaabb")))
(check-sat)
