(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (str.to_re "C") (re.+ (str.to_re "["))))))
(assert (str.in_re y (re.+ (re.++ (str.to_re "d") (re.+ (str.to_re "["))))))
(assert (= (str.to_int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 2))
(check-sat)
