(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "#") (re.* (re.* (str.to_re "'\\t'")))))))
(assert (str.in_re y (re.* (re.++ (str.to_re "#") (str.to_re "'\\t'")))))
(check-sat)
