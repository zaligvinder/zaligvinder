(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.* (str.to_re "ab")))))
(assert (str.in_re y (str.to_re "ab")))
(assert (= 4 2))
(assert (= (str.len y) (str.len x)))
(check-sat)
