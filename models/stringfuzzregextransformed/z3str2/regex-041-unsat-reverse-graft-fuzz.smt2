(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (re.+ (str.to_re "")) (str.to_re "0"))))
(assert (str.in_re x (re.++ (re.+ (str.to_re "c")) (str.to_re "l"))))
(assert (str.in_re x (re.++ (str.to_re "p") (re.union (re.* (str.to_re "c")) (re.* (str.to_re "p"))))))
(check-sat)
