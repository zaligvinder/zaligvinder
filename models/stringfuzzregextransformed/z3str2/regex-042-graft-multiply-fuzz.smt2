(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (re.union (re.+ (str.to_re ";B")) (re.* (str.to_re "c\\"))) (re.* (str.to_re "bb")))))
(assert (str.in_re x (re.++ (str.to_re "n#a") (str.to_re "aa"))))
(assert (= (str.to_int x) 5))
(check-sat)
