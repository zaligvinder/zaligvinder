(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "RR")) (str.to_re "ee")))))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "RR")) (str.to_re "ee")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
