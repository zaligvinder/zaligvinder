(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "DD;;tt;;"))))
(assert (str.in_re x (re.* (str.to_re "DD;;tt;;DD;;tt;;"))))
(assert (str.in_re x (re.* (str.to_re "DD;;tt;;DD;;tt;;DD&&"))))
(assert (> (str.len x) 2))
(check-sat)
