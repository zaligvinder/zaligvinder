(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "21ba")))
(assert (str.in_re y (re.* (re.* (re.* (str.to_re "21ba"))))))
(assert (= (str.len x) 4))
(assert (= 8 (str.len y)))
(check-sat)
