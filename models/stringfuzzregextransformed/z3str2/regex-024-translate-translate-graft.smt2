(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "??")) (str.to_re "S")))))
(assert (str.in_re y (re.* (re.++ (str.to_re "??") (str.to_re "S")))))
(check-sat)
