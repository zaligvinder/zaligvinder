(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "") (re.* (str.to_re "*Wa")))))
(assert (str.in_re x (re.union (str.to_re "cc") (re.* (str.to_re "'3!")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "cc"))) (re.union (str.to_re "S\\b") (str.to_re "e7m")))))
(check-sat)
