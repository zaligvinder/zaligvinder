(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "b")) (str.to_re "a")))))
(assert (= 3 (str.len x)))
(assert (not (= x "baa")))
(assert (not (= x "bba")))
(assert (not (= x "bab")))
(check-sat)
