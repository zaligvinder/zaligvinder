(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "-z?Q"))))
(assert (str.in_re x (re.* (str.to_re "-z?Q-z?Q"))))
(assert (> (str.len x) 20))
(assert (< (str.len x) 25))
(check-sat)
