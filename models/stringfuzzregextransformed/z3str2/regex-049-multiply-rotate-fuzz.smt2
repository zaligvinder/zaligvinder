(set-logic ALL)
(declare-const key String)
(declare-const val String)
(assert (str.in_re key (re.+ (re.range "$" "+"))))
(assert (<= 18 (str.to_int key)))
(assert (>= 20 (str.to_int key)))
(check-sat)
