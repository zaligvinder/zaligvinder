(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "bb")) (str.to_re "{ia"))))
(assert (str.in_re x (re.union (re.++ (re.+ (str.to_re "x")) (re.* (str.to_re "b_"))) (str.to_re "S"))))
(assert (= 1 (str.len x)))
(check-sat)
