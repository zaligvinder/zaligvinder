(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.* (str.to_re "a")) (str.to_re "Ybb")))))
(assert (str.in_re y (re.* (re.union (re.+ (str.to_re """P'\r'asZh")) (str.to_re "bb'\t'UM")))))
(check-sat)
