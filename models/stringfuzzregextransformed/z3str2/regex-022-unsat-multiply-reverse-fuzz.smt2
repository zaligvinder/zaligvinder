(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "bb") (re.* (str.to_re "F,"))))))
(assert (= (str.len x) 1))
(assert (not (= x "jekmm[bb")))
(assert (not (= x "bG)08a")))
(check-sat)
