(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "ww") (re.* (str.to_re "^^"))))))
(assert (= (str.len x) 4))
(assert (not (= x "wwww")))
(assert (not (= x "ww^^")))
(check-sat)
