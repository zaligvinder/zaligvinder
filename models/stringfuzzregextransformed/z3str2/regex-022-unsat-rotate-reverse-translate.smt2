(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "W") (re.* (str.to.re "O"))))))
(assert (= (str.len x) 2))
(assert (not (= x "OO")))
(assert (not (= x "OW")))
(check-sat)
