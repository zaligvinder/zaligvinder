(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "W") (re.* (str.to_re "O"))))))
(assert (= (str.len x) 2))
(assert (not (= x "OO")))
(assert (not (= x "OW")))
(check-sat)
