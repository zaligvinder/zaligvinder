(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "l") (re.+ (str.to_re "b"))))))
(assert (= (str.len x) 4))
(assert (not (= x "aKZ(")))
(assert (not (= x "ba")))
(assert (not (= x "b^KBb")))
(check-sat)
