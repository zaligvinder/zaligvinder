(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "") (re.+ (str.to_re "r"))))))
(assert (= (str.len x) 4))
(assert (not (= x "bNOA")))
(assert (not (= x "k`_a")))
(assert (not (= x "/a")))
(check-sat)
