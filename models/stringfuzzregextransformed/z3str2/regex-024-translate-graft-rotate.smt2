(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "$")) (str.to_re "y")))))
(assert (str.in_re y (re.* (re.++ (str.to_re "y") (str.to_re "$")))))
(check-sat)
