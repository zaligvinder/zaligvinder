(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.+ (str.to_re "")) (str.to_re "")))))
(assert (= (str.len x) 1))
(assert (not (= x "aab")))
(assert (not (= x "yOb")))
(assert (not (= x "5&%t6")))
(check-sat)
