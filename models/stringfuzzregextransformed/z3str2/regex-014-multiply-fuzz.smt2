(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "x9A$+nb"))))
(assert (str.in_re y (re.* (str.to_re "ao"))))
(assert (= (str.len x) 4))
(assert (= (str.len y) 15))
(check-sat)
