(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (= (str.++ y x) (str.++ n m)))
(assert (str.in_re n (re.* (str.to_re "^bI'\\u{b}'}"))))
(assert (= (str.to_int x) (str.len m)))
(check-sat)
