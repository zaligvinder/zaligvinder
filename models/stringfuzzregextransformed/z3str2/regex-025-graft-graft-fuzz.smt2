(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "v") (str.to_re "")))))
(assert (str.in_re y (re.+ (re.+ (str.to_re "a")))))
(assert (= (str.to_int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.to_int x) 8))
(check-sat)
