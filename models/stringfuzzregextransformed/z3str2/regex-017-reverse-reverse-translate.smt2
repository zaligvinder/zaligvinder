(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "<E") (re.union (str.to_re "(neg") (str.to_re "123"))))))
(assert (= 5 (str.len x)))
(check-sat)
