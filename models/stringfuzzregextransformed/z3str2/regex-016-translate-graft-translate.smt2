(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "H?nP"))))
(assert (= (str.len x) 11))
(assert (not (= x "H?nP123H?nP")))
(assert (not (= x "H?nPH?nP123")))
(check-sat)
