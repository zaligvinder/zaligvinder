(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "aabbccddaabbccdd")))
(assert (str.in_re x (str.to_re "aabbccdd")))
(assert (> 40 50))
(assert (< (str.len x) (str.len x)))
(check-sat)
