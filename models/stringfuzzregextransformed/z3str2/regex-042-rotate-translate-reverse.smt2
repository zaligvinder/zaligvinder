(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "m")) (str.to_re "'c0x\\'"))))
(assert (str.in_re x (re.++ (re.++ (str.to_re "m") (str.to_re "'c0x\\'")) (re.* (re.* (str.to_re "x"))))))
(assert (= 3 (str.len x)))
(check-sat)
