(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "m")) (str.to.re "'c0x\\'"))))
(assert (str.in.re x (re.++ (re.++ (str.to.re "m") (str.to.re "'c0x\\'")) (re.* (re.* (str.to.re "x"))))))
(assert (= 3 (str.len x)))
(check-sat)
