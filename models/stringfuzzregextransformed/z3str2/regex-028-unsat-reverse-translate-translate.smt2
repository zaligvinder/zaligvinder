(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "^^s"))))
(assert (str.in_re x (re.* (str.to_re "^^s^^s"))))
(assert (str.in_re x (re.* (str.to_re "' 's^^s^^s"))))
(assert (> (str.len x) 1))
(check-sat)
