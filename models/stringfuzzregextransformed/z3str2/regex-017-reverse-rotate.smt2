(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "dcba") (re.union (str.to_re "321") (str.to_re "BA"))))))
(assert (= 5 (str.len x)))
(check-sat)
