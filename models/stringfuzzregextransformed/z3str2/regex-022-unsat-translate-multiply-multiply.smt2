(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "DDDD")) (str.to_re "tttt")))))
(assert (= (str.len x) 8))
(assert (not (= x "tttttttt")))
(assert (not (= x "DDDDtttt")))
(check-sat)
