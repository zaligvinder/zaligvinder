(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "bb")) (str.to_re "WW")))))
(assert (= (str.to_int x) 0))
(assert (not (= x "aabb[[")))
(assert (not (= x "HHbb")))
(assert (not (= x "aa")))
(check-sat)
