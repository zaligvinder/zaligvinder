(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.* (str.to.re "bb")) (str.to.re "WW")))))
(assert (= (str.to.int x) 0))
(assert (not (= x "aabb[[")))
(assert (not (= x "HHbb")))
(assert (not (= x "aa")))
(check-sat)
