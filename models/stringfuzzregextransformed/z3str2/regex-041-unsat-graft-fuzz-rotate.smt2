(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "b") (re.* (str.to_re "X")))))
(assert (str.in_re x (re.union (str.to_re "c") (re.* (str.to_re "O")))))
(assert (str.in_re x (re.union (re.+ (str.to_re "c")) (re.++ (str.to_re "") (str.to_re ")")))))
(check-sat)
