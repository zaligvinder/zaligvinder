(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "ae") (re.union (str.to_re "$Uw") (str.to_re "AB"))))))
(assert (= 10 (str.len x)))
(check-sat)
