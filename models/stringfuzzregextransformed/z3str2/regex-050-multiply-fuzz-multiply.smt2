(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (= (str.to_int x) 16))
(assert (= x y))
(assert (str.in_re y (re.+ (re.range "X" "b"))))
(assert (str.suffixof "" x))
(check-sat)
