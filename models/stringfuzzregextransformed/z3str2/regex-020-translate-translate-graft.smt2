(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "z") (re.* (str.to_re "["))))))
(assert (= 3 (str.len x)))
(assert (not (= x "[zz")))
(assert (not (= x "z[z")))
(assert (not (= x "[[z")))
(check-sat)
