(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.++ (re.* (str.to_re "b")) (re.* (str.to_re "c"))) (str.to_re "a"))))
(assert (str.in_re x (re.* (str.to_re "b"))))
(assert (= (str.len x) 3))
(check-sat)
