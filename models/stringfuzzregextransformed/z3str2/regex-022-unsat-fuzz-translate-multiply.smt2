(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.+ (str.to_re "--")) (str.to_re "CC")))))
(assert (= (str.to_int x) 8))
(assert (not (= x "00")))
(assert (not (= x "%%")))
(check-sat)
