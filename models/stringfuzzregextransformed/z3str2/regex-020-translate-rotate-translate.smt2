(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "~")) (str.to_re "auu,a")))))
(assert (= (str.len x) 3))
(assert (not (= x "auu,a~~")))
(assert (not (= x "~auu,a~")))
(assert (not (= x "auu,aauu,a~")))
(check-sat)
