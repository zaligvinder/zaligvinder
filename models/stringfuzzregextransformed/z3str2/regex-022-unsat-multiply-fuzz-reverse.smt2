(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "") (re.+ (str.to_re "1"))))))
(assert (= (str.len x) 7))
(assert (not (= x "b/PE`")))
(assert (not (= x "bba""""M")))
(check-sat)
