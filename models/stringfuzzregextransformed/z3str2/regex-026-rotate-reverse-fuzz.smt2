(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "a") (re.+ (str.to_re "b"))))))
(assert (str.in_re y (re.* (re.union (str.to_re "G") (re.+ (str.to_re "'\r'"))))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
