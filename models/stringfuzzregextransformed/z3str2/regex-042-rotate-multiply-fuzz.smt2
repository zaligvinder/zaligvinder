(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "c") (re.+ (str.to_re "aa")))))
(assert (str.in_re x (re.++ (re.+ (re.* (str.to_re "*kK"))) (re.union (str.to_re "-b") (str.to_re "x")))))
(assert (= 2 (str.to_int x)))
(check-sat)
