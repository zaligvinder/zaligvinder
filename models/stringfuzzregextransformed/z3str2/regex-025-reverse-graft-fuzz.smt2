(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "b") (re.* (str.to_re ""))))))
(assert (str.in_re y (re.* (str.to_re "p"))))
(assert (= (str.len x) 5))
(assert (not (= x y)))
(assert (= (str.to_int x) (str.len y)))
(check-sat)
