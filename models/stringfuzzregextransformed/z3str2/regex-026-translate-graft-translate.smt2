(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "N")) (str.to_re "J")))))
(assert (str.in_re y (re.* (re.++ (str.to_re "J") (re.* (str.to_re "N"))))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
