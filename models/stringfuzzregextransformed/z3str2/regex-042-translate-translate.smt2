(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "j") (re.* (str.to_re "e")))))
(assert (str.in_re x (re.++ (str.to_re "j") (re.++ (re.* (str.to_re "e")) (re.* (str.to_re "'\t'"))))))
(assert (= 3 (str.len x)))
(check-sat)
