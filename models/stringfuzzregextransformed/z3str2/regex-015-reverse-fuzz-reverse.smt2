(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "a'c0x\\'VXHY"))))
(assert (str.in_re y (re.* (re.* (str.to_re "W;a'' '''n\\'$O6")))))
(assert (= (str.len x) 3))
(assert (= (str.to_int y) 14))
(check-sat)
