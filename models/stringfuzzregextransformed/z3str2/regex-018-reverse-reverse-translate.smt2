(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "?O") (str.to_re "123")))))
(assert (= 5 (str.len x)))
(assert (not (= x "123?O")))
(check-sat)
