(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "bb") (re.* (str.to_re "aa"))))))
(assert (= (str.len x) 6))
(assert (not (= x "bbbbaa")))
(assert (not (= x "bbaabb")))
(assert (not (= x "bbaaaa")))
(check-sat)
