(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "\\H<0.\\") (re.* (str.to_re "Z")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "<"))) (re.++ (str.to_re "\\H<0.\\") (str.to_re "Z")))))
(assert (= 3 (str.len x)))
(check-sat)
