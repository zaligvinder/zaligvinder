(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "jj") (re.* (str.to_re "qq")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "LL"))) (re.++ (str.to_re "jj") (str.to_re "qq")))))
(assert (= 6 (str.len x)))
(check-sat)
