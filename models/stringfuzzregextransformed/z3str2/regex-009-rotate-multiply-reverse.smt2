(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "ddccbbaa"))))
(assert (str.in_re x (re.* (str.to_re "ddccbbaaddccbbaa"))))
(assert (> (str.len x) 40))
(assert (< (str.len x) 50))
(check-sat)
