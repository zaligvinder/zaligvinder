(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "'\t'") (re.* (str.to_re "]"))))))
(assert (= 2 (str.len x)))
(assert (not (= x "]]")))
(assert (not (= x "'\t']")))
(check-sat)
