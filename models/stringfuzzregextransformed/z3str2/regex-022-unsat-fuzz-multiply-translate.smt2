(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "zz")) (str.to_re "<<")))))
(assert (= (str.to_int x) 6))
(assert (not (= x "<<<<")))
(assert (not (= x "\\\\")))
(check-sat)
