(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "b") (re.* (str.to_re "a"))))))
(assert (= (str.len x) 3))
(assert (not (= x "baa")))
(assert (not (= x "bba")))
(assert (not (= x "bab")))
(check-sat)
