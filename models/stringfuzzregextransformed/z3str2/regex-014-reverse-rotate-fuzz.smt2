(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "ba"))))
(assert (str.in_re y (re.+ (str.to_re "2jy"))))
(assert (= (str.len x) 0))
(assert (= (str.to_int y) 4))
(check-sat)
