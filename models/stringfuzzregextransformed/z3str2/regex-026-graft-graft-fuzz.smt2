(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (re.+ (str.to_re "")) (re.+ (str.to_re "V"))))))
(assert (str.in_re y (str.to_re "")))
(assert (not (= x y)))
(assert (= (str.len x) (str.to_int y)))
(check-sat)
