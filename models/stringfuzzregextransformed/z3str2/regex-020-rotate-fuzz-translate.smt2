(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "s")) (str.to_re "\\")))))
(assert (= (str.to_int x) 0))
(assert (not (= x "us]")))
(assert (not (= x "Bs")))
(assert (not (= x "u")))
(check-sat)
