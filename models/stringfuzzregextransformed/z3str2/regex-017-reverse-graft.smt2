(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.union (str.to_re "dcba") (str.to_re "321")) (str.to_re "BA")))))
(assert (= (str.len x) 5))
(check-sat)
