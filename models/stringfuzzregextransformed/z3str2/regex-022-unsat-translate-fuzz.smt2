(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "D")) (str.to_re "h")))))
(assert (= (str.to_int x) 3))
(assert (not (= x "U/")))
(assert (not (= x "D")))
(check-sat)
