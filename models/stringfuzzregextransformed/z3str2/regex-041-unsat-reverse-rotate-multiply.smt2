(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "aa")) (str.to_re "bb"))))
(assert (str.in_re x (re.++ (re.* (str.to_re "cc")) (str.to_re "cc"))))
(assert (str.in_re x (re.++ (re.* (str.to_re "bb")) (re.* (re.++ (str.to_re "cc") (str.to_re "aa"))))))
(check-sat)
