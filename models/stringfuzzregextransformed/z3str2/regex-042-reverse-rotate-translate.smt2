(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "b")) (str.to.re "}"))))
(assert (str.in.re x (re.++ (re.* (str.to.re "}")) (re.* (re.++ (str.to.re "j") (str.to.re "b"))))))
(assert (= 3 (str.len x)))
(check-sat)
