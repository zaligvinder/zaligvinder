(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "tt")) (str.to_re "DD")))))
(assert (= (str.len x) 4))
(assert (not (= x "tttt")))
(assert (not (= x "DDtt")))
(check-sat)
