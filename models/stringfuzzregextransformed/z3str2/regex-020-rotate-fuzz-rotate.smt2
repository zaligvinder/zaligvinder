(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "W")) (str.to_re "b")))))
(assert (= (str.to_int x) 0))
(assert (not (= x "ab[")))
(assert (not (= x "Hb")))
(assert (not (= x "a")))
(check-sat)
