(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (= (str.to_int x) 5))
(assert (= x y))
(assert (str.in_re y (re.* (re.range "a" "h"))))
(assert (str.suffixof "1" x))
(check-sat)
