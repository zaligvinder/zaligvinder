(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "bw/") (re.* (str.to.re "amm"))))))
(assert (str.in.re y (re.* (re.union (str.to.re "j") (re.+ (str.to.re "axm"))))))
(assert (= (str.len x) (str.to.int y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 28))
(check-sat)
