(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "*")) (str.to_re "X"))))
(assert (str.in_re x (re.++ (re.* (str.to_re "X")) (re.* (re.++ (str.to_re "$") (str.to_re "*"))))))
(assert (= 3 (str.len x)))
(check-sat)
