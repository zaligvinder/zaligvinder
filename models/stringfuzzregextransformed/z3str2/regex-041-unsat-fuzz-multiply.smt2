(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "aa") (re.+ (str.to_re "bb")))))
(assert (str.in_re x (re.union (str.to_re "KK") (re.* (str.to_re "SS")))))
(assert (str.in_re x (re.++ (str.to_re "XX") (re.++ (re.* (str.to_re "rr")) (re.+ (str.to_re "::"))))))
(check-sat)
