(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.+ (str.to_re "'\u{c}'")) (str.to_re "")))))
(assert (= (str.len x) 1))
(assert (not (= x "LF+")))
(assert (not (= x "+")))
(check-sat)
