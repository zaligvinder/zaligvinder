(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "[") (re.* (str.to_re "j"))))))
(assert (= (str.len x) 3))
(assert (not (= x "jj[")))
(assert (not (= x "j[j")))
(assert (not (= x "j[[")))
(check-sat)
