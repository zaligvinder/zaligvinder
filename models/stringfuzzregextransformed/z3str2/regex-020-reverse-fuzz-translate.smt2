(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "n") (re.+ (str.to_re ""))))))
(assert (= (str.len x) 4))
(assert (not (= x "{gUF")))
(assert (not (= x "Wt\\""")))
(assert (not (= x "X""")))
(check-sat)
