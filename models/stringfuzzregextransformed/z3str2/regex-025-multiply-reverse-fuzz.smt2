(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "b") (re.* (str.to_re "b<"))))))
(assert (str.in_re y (re.+ (re.union (str.to_re "b") (re.* (str.to_re "ja"))))))
(assert (= (str.to_int x) (str.to_int y)))
(assert (not (= x y)))
(assert (= (str.len x) 22))
(check-sat)
