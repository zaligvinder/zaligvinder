(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "21b't\\'|~7"))))
(assert (str.in_re y (re.* (re.+ (str.to_re "1a")))))
(assert (= (str.len x) 0))
(assert (= (str.len y) 14))
(check-sat)
