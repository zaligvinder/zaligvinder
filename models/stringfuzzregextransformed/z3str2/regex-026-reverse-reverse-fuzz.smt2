(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (re.* (str.to_re "a")) (str.to_re "k")))))
(assert (str.in_re y (re.* (re.union (re.* (str.to_re "e")) (str.to_re "b")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
