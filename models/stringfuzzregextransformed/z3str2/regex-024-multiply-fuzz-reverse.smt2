(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "bb") (re.+ (str.to_re "a"))))))
(assert (str.in_re y (re.* (re.++ (str.to_re "#") (re.+ (str.to_re "pE"))))))
(check-sat)
