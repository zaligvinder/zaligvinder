(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "1") (re.+ (str.to_re "b"))))))
(assert (= 4 (str.len x)))
(assert (not (= x "b")))
(assert (not (= x "a")))
(check-sat)
