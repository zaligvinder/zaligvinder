(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "a}'\n'lcd"))))
(assert (str.in_re y (re.* (str.to_re "aeb:$cdd"))))
(assert (= (str.len x) 11))
(check-sat)
