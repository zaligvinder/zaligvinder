(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (str.to_re "B*") (re.++ (str.to_re "P~j") (str.to_re "31"))))))
(assert (= 4 (str.len x)))
(check-sat)
