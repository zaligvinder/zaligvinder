(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "f") (re.* (str.to_re ";tXX;"))))))
(assert (= (str.len x) 3))
(assert (not (= x "ff;tXX;")))
(assert (not (= x "f;tXX;f")))
(assert (not (= x "f;tXX;;tXX;")))
(check-sat)
