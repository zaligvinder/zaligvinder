(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "b") (re.* (str.to_re "0"))))))
(assert (str.in_re y (re.* (re.union (str.to_re "b") (re.+ (str.to_re "G"))))))
(assert (not (= x y)))
(assert (= (str.len x) (str.to_int y)))
(check-sat)
