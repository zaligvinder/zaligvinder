(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.* (str.to_re "")) (str.to_re "b")))))
(assert (str.in_re y (str.to_re "")))
(assert (= (str.to_int x) 3))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
