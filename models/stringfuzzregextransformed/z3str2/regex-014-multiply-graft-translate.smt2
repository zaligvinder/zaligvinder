(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "bbll")))
(assert (str.in_re y (re.* (re.* (str.to_re "bbll")))))
(assert (= (str.len x) 4))
(assert (= 8 (str.len y)))
(check-sat)
