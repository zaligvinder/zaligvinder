(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "dc") (re.union (str.to_re "%r2:") (str.to_re "BA"))))))
(assert (= 1 (str.to_int x)))
(check-sat)
