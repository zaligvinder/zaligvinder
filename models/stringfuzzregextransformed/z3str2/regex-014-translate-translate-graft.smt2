(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "*+")))
(assert (str.in_re y (re.* (re.* (str.to_re "*+")))))
(assert (= (str.len x) 2))
(assert (= 4 (str.len y)))
(check-sat)
