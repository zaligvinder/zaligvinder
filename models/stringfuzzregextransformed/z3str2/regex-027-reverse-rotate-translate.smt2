(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "g") (re.* (str.to_re "-"))))))
(assert (= (str.len x) 3))
(assert (not (= x "-gg")))
(assert (not (= x "--g")))
(assert (not (= x "-g-")))
(check-sat)
