(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.* (str.to_re "y")) (str.to_re "HFv80")))))
(assert (str.in_re y (re.+ (re.union (re.+ (str.to_re ":")) (str.to_re "7L30'\u{c}'{^gH")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.to_int y)))
(check-sat)
