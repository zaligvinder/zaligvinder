(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "aaaa") (re.* (str.to_re "bbbb")))))
(assert (str.in_re x (re.union (str.to_re "") (re.+ (str.to_re "cccc")))))
(assert (str.in_re x (re.++ (str.to_re "aaaa") (re.union (re.* (str.to_re "bbbb")) (re.* (str.to_re ""))))))
(check-sat)
