(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "#")) (str.to_re "C")))))
(assert (str.in_re y (re.* (re.union (re.* (str.to_re "a")) (str.to_re "b")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.to_int x) 4))
(check-sat)
