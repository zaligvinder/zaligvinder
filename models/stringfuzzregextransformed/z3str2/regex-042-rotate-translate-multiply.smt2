(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "''\\\\u{x0}0cc''") (re.* (str.to_re "mm")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "xx"))) (re.++ (str.to_re "''\\\\u{x0}0cc''") (str.to_re "mm")))))
(assert (= 6 (str.len x)))
(check-sat)
