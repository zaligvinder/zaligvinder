(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "''\\\\xx00cc''") (re.* (str.to.re "mm")))))
(assert (str.in.re x (re.++ (re.* (re.* (str.to.re "xx"))) (re.++ (str.to.re "''\\\\xx00cc''") (str.to.re "mm")))))
(assert (= 6 (str.len x)))
(check-sat)
