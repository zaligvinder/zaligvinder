(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re ">")) (re.* (str.to.re ">"))))))
(assert (str.in.re y (re.* (re.++ (str.to.re "p") (str.to.re "p")))))
(assert (= 7 (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) (str.len x)))
(check-sat)
