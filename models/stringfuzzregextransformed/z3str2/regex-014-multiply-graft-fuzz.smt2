(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "2")))
(assert (str.in_re y (re.* (re.+ (str.to_re "abb")))))
(assert (= (str.len x) 6))
(assert (= 6 (str.to_int y)))
(check-sat)
