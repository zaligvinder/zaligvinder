(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "..") (re.* (str.to_re "pp"))))))
(assert (= (str.len x) 6))
(assert (not (= x "..pppp")))
(assert (not (= x "....pp")))
(assert (not (= x "..pp..")))
(check-sat)
