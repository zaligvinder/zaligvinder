(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "..") (re.* (str.to.re "pp"))))))
(assert (= (str.len x) 6))
(assert (not (= x "..pppp")))
(assert (not (= x "....pp")))
(assert (not (= x "..pp..")))
(check-sat)
