(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "t") (re.+ (str.to_re "9")))))
(assert (str.in_re x (re.union (str.to_re "c") (re.* (re.+ (re.++ (str.to_re "b") (str.to_re "a")))))))
(assert (= 1 (str.len x)))
(check-sat)
