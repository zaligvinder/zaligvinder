(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "a-[BD"))))
(assert (str.in_re x (re.+ (str.to_re "bcd'\t'86cb~' '"))))
(assert (> (str.to_int x) 19))
(assert (< (str.to_int x) 24))
(check-sat)
