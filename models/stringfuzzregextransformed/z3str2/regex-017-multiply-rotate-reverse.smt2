(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "ddccbbaa") (re.union (str.to_re "332211") (str.to_re "BBAA"))))))
(assert (= 10 (str.len x)))
(check-sat)
