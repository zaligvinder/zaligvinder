(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "F") (re.* (str.to_re "a")))))
(assert (str.in_re x (re.union (str.to_re "c") (re.+ (str.to_re "")))))
(assert (str.in_re x (re.union (str.to_re "a") (re.union (str.to_re "") (re.* (re.* (str.to_re "c")))))))
(check-sat)
