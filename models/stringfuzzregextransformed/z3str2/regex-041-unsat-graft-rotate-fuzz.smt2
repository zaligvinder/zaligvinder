(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "J") (re.* (str.to_re "q")))))
(assert (str.in_re x (re.union (str.to_re "") (re.+ (str.to_re "g")))))
(assert (str.in_re x (re.++ (re.+ (str.to_re "$")) (re.++ (str.to_re "b") (str.to_re "")))))
(check-sat)
