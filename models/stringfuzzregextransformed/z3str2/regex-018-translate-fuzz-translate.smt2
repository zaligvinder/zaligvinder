(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "'\u{b}'") (str.to_re "")))))
(assert (= 1 (str.to_int x)))
(assert (not (= x "$+K+63&v\\(")))
(check-sat)
