(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "b")) (str.to_re ""))))
(assert (str.in_re x (re.union (re.union (str.to_re ")") (re.* (str.to_re "b"))) (re.+ (str.to_re "")))))
(assert (= (str.to_int x) 1))
(check-sat)
