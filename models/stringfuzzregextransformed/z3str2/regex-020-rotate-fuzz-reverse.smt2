(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "b")) (str.to_re "W")))))
(assert (= (str.to_int x) 0))
(assert (not (= x "[ba")))
(assert (not (= x "bH")))
(assert (not (= x "a")))
(check-sat)
