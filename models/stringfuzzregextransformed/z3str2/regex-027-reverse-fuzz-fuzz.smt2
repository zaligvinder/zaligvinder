(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "T") (re.* (str.to_re ""))))))
(assert (= (str.len x) 0))
(assert (not (= x ">g9l0")))
(assert (not (= x "8bco?")))
(assert (not (= x "b")))
(check-sat)
