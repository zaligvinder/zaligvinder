(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "cc")) (str.to_re "!!")))))
(assert (= (str.len x) 6))
(assert (not (= x "!!!!cc")))
(assert (not (= x "!!cccc")))
(assert (not (= x "cc!!cc")))
(check-sat)
