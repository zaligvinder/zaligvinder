(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "cc")) (str.to.re "!!")))))
(assert (= (str.len x) 6))
(assert (not (= x "!!!!cc")))
(assert (not (= x "!!cccc")))
(assert (not (= x "cc!!cc")))
(check-sat)
