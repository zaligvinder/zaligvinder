(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "{{")) (str.to_re "LL")))))
(assert (= (str.len x) 6))
(assert (not (= x "{{LLLL")))
(assert (not (= x "LL{{LL")))
(assert (not (= x "{{{{LL")))
(check-sat)
