(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "ab"))))
(assert (str.in_re y (re.* (str.to_re "aRb"))))
(assert (= (str.to_int x) 7))
(assert (= (str.len y) 9))
(check-sat)
