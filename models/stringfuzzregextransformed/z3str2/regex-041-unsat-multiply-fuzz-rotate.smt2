(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "bb") (re.* (str.to_re "aw")))))
(assert (str.in_re x (re.union (str.to_re "RZK") (re.+ (str.to_re "c")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "ch@"))) (re.++ (str.to_re "4'\\u{c}'") (str.to_re "a")))))
(check-sat)
