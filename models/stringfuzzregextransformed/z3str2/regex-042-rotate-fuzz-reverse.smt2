(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "9") (re.+ (str.to_re "t")))))
(assert (str.in_re x (re.union (re.+ (re.* (str.to_re "c"))) (re.++ (str.to_re "a") (str.to_re "b")))))
(assert (= 1 (str.len x)))
(check-sat)
