(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "aa")) (str.to_re "bb")))))
(assert (= (str.len x) 4))
(assert (not (= x "bbbb")))
(assert (not (= x "aabb")))
(check-sat)
