(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (str.to.re "bbbb")))
(assert (str.in.re y (re.* (re.++ (re.* (str.to.re "aaaa")) (re.* (re.++ (re.* (str.to.re "aaaa")) (str.to.re "bbbb")))))))
(check-sat)
