(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "bbbb")))
(assert (str.in_re y (re.* (re.++ (re.* (str.to_re "aaaa")) (re.* (re.++ (re.* (str.to_re "aaaa")) (str.to_re "bbbb")))))))
(check-sat)
