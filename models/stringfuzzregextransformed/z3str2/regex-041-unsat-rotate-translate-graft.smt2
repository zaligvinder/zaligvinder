(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "/") (re.* (str.to_re "j")))))
(assert (str.in_re x (re.++ (str.to_re "R") (re.* (str.to_re "R")))))
(assert (str.in_re x (re.++ (re.* (str.to_re "j")) (re.++ (str.to_re "/") (re.* (str.to_re "R"))))))
(check-sat)
