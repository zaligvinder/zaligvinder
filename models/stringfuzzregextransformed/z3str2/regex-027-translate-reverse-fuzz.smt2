(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "J") (re.* (str.to_re "8"))))))
(assert (= (str.to_int x) 4))
(assert (not (= x "J")))
(assert (not (= x "+ww")))
(assert (not (= x "&")))
(check-sat)
