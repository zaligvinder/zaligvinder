(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "****") (re.* (str.to_re "XXXX")))))
(assert (str.in_re x (re.++ (str.to_re "****") (re.++ (re.* (str.to_re "XXXX")) (re.* (str.to_re "$$$$"))))))
(assert (= 12 (str.len x)))
(check-sat)
