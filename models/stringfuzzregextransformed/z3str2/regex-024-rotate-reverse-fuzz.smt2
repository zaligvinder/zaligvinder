(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "J") (re.+ (str.to_re ""))))))
(assert (str.in_re y (re.+ (re.++ (str.to_re "O") (re.+ (str.to_re "b"))))))
(check-sat)
