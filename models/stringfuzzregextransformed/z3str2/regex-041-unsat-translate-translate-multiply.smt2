(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "''''") (re.* (str.to_re "kk")))))
(assert (str.in_re x (re.++ (str.to_re "YY") (re.* (str.to_re "YY")))))
(assert (str.in_re x (re.++ (str.to_re "''''") (re.++ (re.* (str.to_re "kk")) (re.* (str.to_re "YY"))))))
(check-sat)
