(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.union (re.+ (str.to_re "ee")) (re.+ (str.to_re "cc"))) (re.* (str.to_re "bb")))))
(assert (str.in_re x (re.union (str.to_re "aa") (str.to_re "GG"))))
(assert (= (str.to_int x) 10))
(check-sat)
