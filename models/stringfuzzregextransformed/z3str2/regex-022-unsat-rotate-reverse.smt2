(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "a") (re.* (str.to_re "b"))))))
(assert (= (str.len x) 2))
(assert (not (= x "bb")))
(assert (not (= x "ba")))
(check-sat)
