(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "OOOO") (re.* (str.to_re "SSSS")))))
(assert (str.in_re x (re.++ (str.to_re "vvvv") (re.* (str.to_re "vvvv")))))
(assert (str.in_re x (re.++ (str.to_re "OOOO") (re.++ (re.* (str.to_re "SSSS")) (re.* (str.to_re "vvvv"))))))
(check-sat)
