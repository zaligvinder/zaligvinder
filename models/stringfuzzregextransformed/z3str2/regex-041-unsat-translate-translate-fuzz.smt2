(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "%1") (re.* (str.to_re "")))))
(assert (str.in_re x (re.union (str.to_re "Y") (re.+ (str.to_re "")))))
(assert (str.in_re x (re.++ (str.to_re "''") (re.++ (re.* (str.to_re "%")) (re.* (str.to_re "'\t'"))))))
(check-sat)
