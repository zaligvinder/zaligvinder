(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.+ (str.to_re "I")) (str.to_re "R")))))
(assert (= (str.len x) 0))
(assert (not (= x "-6")))
(assert (not (= x "bYz")))
(assert (not (= x "ab")))
(check-sat)
