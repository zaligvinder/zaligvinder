(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (= (str.++ x y) (str.++ m n)))
(assert (str.in_re n (re.* (str.to_re "jbcy4j"))))
(assert (> (str.len x) (str.len m)))
(assert (= 2 (str.len n)))
(assert (= 3 (str.to_int y)))
(check-sat)
