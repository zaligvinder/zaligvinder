(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "UU")) (str.to.re "TT")))))
(assert (= (str.len x) 6))
(assert (not (= x "UUUUTT")))
(assert (not (= x "UUTTTT")))
(assert (not (= x "TTUUTT")))
(check-sat)
