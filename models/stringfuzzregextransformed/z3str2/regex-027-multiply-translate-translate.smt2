(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "UU")) (str.to_re "TT")))))
(assert (= (str.len x) 6))
(assert (not (= x "UUUUTT")))
(assert (not (= x "UUTTTT")))
(assert (not (= x "TTUUTT")))
(check-sat)
