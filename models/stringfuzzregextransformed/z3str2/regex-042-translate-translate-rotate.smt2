(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "e") (re.* (str.to_re "j")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "'\\t'"))) (re.++ (str.to_re "e") (str.to_re "j")))))
(assert (= 3 (str.len x)))
(check-sat)
