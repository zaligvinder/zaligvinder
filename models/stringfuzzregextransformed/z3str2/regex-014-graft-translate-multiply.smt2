(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "pp<<"))))
(assert (str.in_re y (str.to_re "pp<<")))
(assert (= 4 (str.len x)))
(assert (= (str.len y) 8))
(check-sat)
