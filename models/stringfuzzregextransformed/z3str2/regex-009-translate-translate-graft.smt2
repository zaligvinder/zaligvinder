(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.* (str.to_re "$|+g$|+g")))))
(assert (str.in_re x (str.to_re "$|+g")))
(assert (> (str.len x) (str.len x)))
(assert (< 20 25))
(check-sat)
