(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "fM"))))
(assert (str.in_re y (re.+ (str.to_re "sWb"))))
(assert (= (str.len x) 2))
(assert (= (str.len y) 8))
(check-sat)
