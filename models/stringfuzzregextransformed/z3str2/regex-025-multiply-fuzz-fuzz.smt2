(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.* (str.to_re "")) (str.to_re "U\\.[ax'\u{b}'")))))
(assert (str.in_re y (re.+ (re.union (re.+ (str.to_re "Ea")) (str.to_re "")))))
(assert (= (str.to_int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 9))
(check-sat)
