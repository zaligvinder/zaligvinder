(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "wwww")) (str.to_re "DDDD")))))
(assert (= (str.len x) 8))
(assert (not (= x "DDDDDDDD")))
(assert (not (= x "wwwwDDDD")))
(check-sat)
