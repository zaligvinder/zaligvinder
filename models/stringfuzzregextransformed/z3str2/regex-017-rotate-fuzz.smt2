(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re """w") (re.union (str.to_re "S3") (str.to_re "vk"))))))
(assert (= 2 (str.to_int x)))
(check-sat)
