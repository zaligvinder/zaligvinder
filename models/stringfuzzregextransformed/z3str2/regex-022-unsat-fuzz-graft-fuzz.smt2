(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (str.to_re "") (re.* (str.to_re "a"))))))
(assert (= 6 (str.len x)))
(assert (not (= x "")))
(assert (not (= x "")))
(check-sat)
