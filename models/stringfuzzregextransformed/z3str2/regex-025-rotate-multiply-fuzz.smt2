(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.+ (str.to.re "b)H")) (str.to.re "a")))))
(assert (str.in.re y (re.+ (re.union (re.* (str.to.re "jx")) (str.to.re "")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 8))
(check-sat)
