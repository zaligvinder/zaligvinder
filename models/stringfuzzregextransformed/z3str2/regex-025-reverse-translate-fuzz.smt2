(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "W") (re.+ (str.to_re ""))))))
(assert (str.in_re y (re.+ (re.union (str.to_re "h") (re.* (str.to_re ""))))))
(assert (= (str.to_int x) (str.to_int y)))
(assert (not (= x y)))
(assert (= (str.to_int x) 9))
(check-sat)
