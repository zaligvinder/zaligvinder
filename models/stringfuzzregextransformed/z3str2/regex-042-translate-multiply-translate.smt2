(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "__") (re.* (str.to_re "jj")))))
(assert (str.in_re x (re.++ (str.to_re "__") (re.++ (re.* (str.to_re "jj")) (re.* (str.to_re "UU"))))))
(assert (= 6 (str.len x)))
(check-sat)
