(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "")) (str.to_re "h"))))
(assert (str.in_re x (re.union (re.* (str.to_re "'\r'")) (str.to_re "c"))))
(assert (str.in_re x (re.++ (re.union (str.to_re "") (re.* (str.to_re "b"))) (str.to_re "a"))))
(check-sat)
