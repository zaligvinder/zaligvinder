(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "bb")) (str.to_re "aa")))))
(assert (= (str.len x) 6))
(assert (not (= x "aaaabb")))
(assert (not (= x "aabbbb")))
(assert (not (= x "bbaabb")))
(check-sat)
