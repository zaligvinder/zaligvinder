(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "b") (str.to_re "b")))))
(assert (str.in_re y (re.* (str.to_re "a"))))
(assert (= (str.len x) (str.len x)))
(assert (not (= x y)))
(assert (= 2 (str.to_int y)))
(check-sat)
