(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "'c0x\\'") (re.* (str.to.re "}"))))))
(assert (= (str.len x) 3))
(assert (not (= x "}}'c0x\\'")))
(assert (not (= x "}'c0x\\'}")))
(assert (not (= x "}'c0x\\''c0x\\'")))
(check-sat)
