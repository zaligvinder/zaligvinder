(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "'c0x\\'") (re.* (str.to_re "}"))))))
(assert (= (str.len x) 3))
(assert (not (= x "}}'c0x\\'")))
(assert (not (= x "}'c0x\\'}")))
(assert (not (= x "}'c0x\\''c0x\\'")))
(check-sat)
