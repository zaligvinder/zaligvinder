(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "tt")) (str.to_re "")))))
(assert (= (str.len x) 12))
(assert (not (= x "##,,??aa")))
(assert (not (= x "aa..kk**bb")))
(assert (not (= x "BB55''' '' '''bb")))
(check-sat)
