(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "V") (re.* (str.to_re "w")))))
(assert (str.in_re x (re.++ (str.to_re "t") (re.++ (re.* (str.to_re "V")) (str.to_re "w")))))
(assert (= (str.len x) 3))
(check-sat)
