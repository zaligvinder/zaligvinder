(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.* (str.to.re "a")) (str.to.re "bb")))))
(assert (str.in.re y (re.* (re.++ (re.+ (str.to.re "aG")) (str.to.re "5yb")))))
(assert (= (str.len x) (str.to.int y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 24))
(check-sat)
