(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "fV") (re.* (str.to_re "L")))))
(assert (str.in_re x (re.union (str.to_re "") (re.+ (str.to_re "$'\r'")))))
(assert (str.in_re x (re.++ (re.* (re.+ (str.to_re "@8c"))) (re.++ (str.to_re "%") (str.to_re "I4a")))))
(check-sat)
