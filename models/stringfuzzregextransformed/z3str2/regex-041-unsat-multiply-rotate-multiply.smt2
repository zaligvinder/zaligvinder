(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "bbbb") (re.* (str.to_re "aaaa")))))
(assert (str.in_re x (re.++ (str.to_re "cccc") (re.* (str.to_re "cccc")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "cccc"))) (re.++ (str.to_re "bbbb") (str.to_re "aaaa")))))
(check-sat)
