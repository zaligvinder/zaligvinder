(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "u") (str.to_re "o")))))
(assert (= 3 (str.len x)))
(assert (not (= x "uuo")))
(assert (not (= x "uou")))
(assert (not (= x "uoo")))
(check-sat)
