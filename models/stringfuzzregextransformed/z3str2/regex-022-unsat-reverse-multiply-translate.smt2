(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "YY") (re.* (str.to_re "ff"))))))
(assert (= (str.len x) 4))
(assert (not (= x "YYYY")))
(assert (not (= x "YYff")))
(check-sat)
