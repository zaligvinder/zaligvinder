(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "%") (re.* (str.to.re "K"))))))
(assert (= (str.len x) 3))
(assert (not (= x "%KK")))
(assert (not (= x "%%K")))
(assert (not (= x "%K%")))
(check-sat)
