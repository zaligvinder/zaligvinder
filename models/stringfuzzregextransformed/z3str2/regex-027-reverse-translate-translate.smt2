(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "%") (re.* (str.to_re "K"))))))
(assert (= (str.len x) 3))
(assert (not (= x "%KK")))
(assert (not (= x "%%K")))
(assert (not (= x "%K%")))
(check-sat)
