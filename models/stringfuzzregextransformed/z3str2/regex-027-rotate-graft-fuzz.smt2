(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "b") (str.to_re "")))))
(assert (= 4 (str.len x)))
(assert (not (= x "P{")))
(assert (not (= x "_elu.")))
(assert (not (= x "a")))
(check-sat)
