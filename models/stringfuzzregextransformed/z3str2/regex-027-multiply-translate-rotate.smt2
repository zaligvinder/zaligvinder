(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "YY")) (str.to_re "CC")))))
(assert (= (str.len x) 6))
(assert (not (= x "CCCCYY")))
(assert (not (= x "CCYYYY")))
(assert (not (= x "YYCCYY")))
(check-sat)
