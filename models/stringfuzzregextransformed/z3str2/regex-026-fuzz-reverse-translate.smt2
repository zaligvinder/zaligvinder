(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "s")) (str.to_re "m")))))
(assert (str.in_re y (re.* (re.union (re.+ (str.to_re "s")) (str.to_re "m")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.to_int y)))
(check-sat)
