(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "aaaabbbb")))
(assert (str.in_re y (re.* (re.* (str.to_re "aaaabbbb")))))
(assert (= (str.len x) 8))
(assert (= 16 (str.len y)))
(check-sat)
