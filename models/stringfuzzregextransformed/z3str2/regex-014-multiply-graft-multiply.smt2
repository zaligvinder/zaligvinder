(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (str.to.re "aaaabbbb")))
(assert (str.in.re y (re.* (re.* (str.to.re "aaaabbbb")))))
(assert (= (str.len x) 8))
(assert (= 16 (str.len y)))
(check-sat)
