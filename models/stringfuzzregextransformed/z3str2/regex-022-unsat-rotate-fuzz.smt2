(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (re.+ (str.to_re "b")) (str.to_re "&")))))
(assert (= (str.to_int x) 4))
(assert (not (= x "bE>")))
(assert (not (= x "ab")))
(check-sat)
