(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.* (re.* (str.to_re "aabb1122"))))))
(assert (str.in_re y (str.to_re "aabb1122")))
(assert (= (str.len x) 8))
(assert (= 16 (str.len y)))
(check-sat)
