(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "hhhh") (re.* (str.to_re "####")))))
(assert (str.in_re x (re.++ (str.to_re "yyyy") (re.* (str.to_re "yyyy")))))
(assert (str.in_re x (re.++ (str.to_re "hhhh") (re.++ (re.* (str.to_re "####")) (re.* (str.to_re "yyyy"))))))
(check-sat)
