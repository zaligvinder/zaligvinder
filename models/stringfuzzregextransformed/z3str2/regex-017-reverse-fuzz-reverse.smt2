(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "\\\\") (re.union (str.to_re "abf_") (str.to_re "1"))))))
(assert (= 0 (str.to_int x)))
(check-sat)
