(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "@") (re.* (str.to_re "'c0x\\'"))))))
(assert (= (str.len x) 2))
(assert (not (= x "'c0x\\''c0x\\'")))
(assert (not (= x "'c0x\\'@")))
(check-sat)
