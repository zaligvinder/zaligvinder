(set-logic ALL)
(declare-const key String)
(declare-const val String)
(assert (str.in_re key (re.* (re.range "a" "b"))))
(assert (<= (str.to_int key) 8))
(assert (>= 12 (str.to_int key)))
(check-sat)
