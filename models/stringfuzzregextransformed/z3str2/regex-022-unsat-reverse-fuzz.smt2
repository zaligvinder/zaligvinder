(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "b") (re.+ (str.to_re "m"))))))
(assert (= (str.len x) 4))
(assert (not (= x "Hb")))
(assert (not (= x "a")))
(check-sat)
