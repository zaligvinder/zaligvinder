(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "i") (re.* (str.to_re "e")))))
(assert (str.in_re x (re.union (re.* (str.to_re "")) (re.+ (str.to_re ">")))))
(assert (str.in_re x (re.++ (str.to_re "i") (re.union (re.* (str.to_re "e")) (str.to_re "")))))
(check-sat)
