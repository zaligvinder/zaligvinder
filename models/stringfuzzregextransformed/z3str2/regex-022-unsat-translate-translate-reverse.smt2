(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "+") (re.* (str.to_re "T"))))))
(assert (= (str.len x) 2))
(assert (not (= x "++")))
(assert (not (= x "+T")))
(check-sat)
