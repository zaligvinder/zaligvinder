(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "OO") (re.* (str.to_re "SS")))))
(assert (str.in_re x (re.++ (str.to_re "vv") (re.* (str.to_re "vv")))))
(assert (str.in_re x (re.++ (str.to_re "OO") (re.++ (re.* (str.to_re "SS")) (re.* (str.to_re "vv"))))))
(check-sat)
