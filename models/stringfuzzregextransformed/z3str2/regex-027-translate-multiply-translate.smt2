(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "mm")) (str.to.re "$$")))))
(assert (= (str.len x) 6))
(assert (not (= x "mmmm$$")))
(assert (not (= x "mm$$$$")))
(assert (not (= x "$$mm$$")))
(check-sat)
