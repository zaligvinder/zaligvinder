(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "mm")) (str.to_re "$$")))))
(assert (= (str.len x) 6))
(assert (not (= x "mmmm$$")))
(assert (not (= x "mm$$$$")))
(assert (not (= x "$$mm$$")))
(check-sat)
