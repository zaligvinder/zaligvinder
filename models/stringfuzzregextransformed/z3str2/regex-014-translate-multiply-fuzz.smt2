(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (str.to_re "p|"))))
(assert (str.in_re y (re.* (str.to_re "!x,z"))))
(assert (= (str.to_int x) 1))
(assert (= (str.len y) 3))
(check-sat)
