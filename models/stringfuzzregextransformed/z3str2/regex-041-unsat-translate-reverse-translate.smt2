(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "))>))")) (str.to_re "'\u{c}'"))))
(assert (str.in_re x (re.++ (re.* (str.to_re "K")) (str.to_re "K"))))
(assert (str.in_re x (re.++ (re.++ (re.* (str.to_re "K")) (re.* (str.to_re "))>))"))) (str.to_re "'\u{c}'"))))
(check-sat)
