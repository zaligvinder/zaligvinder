(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "DD")) (str.to_re "hh")))))
(assert (= (str.len x) 4))
(assert (not (= x "hhhh")))
(assert (not (= x "DDhh")))
(check-sat)
