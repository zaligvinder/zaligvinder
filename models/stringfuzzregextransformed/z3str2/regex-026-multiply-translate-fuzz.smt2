(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (re.+ (str.to.re "")) (str.to.re "ww")))))
(assert (str.in.re y (re.* (re.union (re.* (str.to.re "L")) (str.to.re "wk")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
