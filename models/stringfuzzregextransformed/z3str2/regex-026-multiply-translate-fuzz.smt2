(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.+ (str.to_re "")) (str.to_re "ww")))))
(assert (str.in_re y (re.* (re.union (re.* (str.to_re "L")) (str.to_re "wk")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
