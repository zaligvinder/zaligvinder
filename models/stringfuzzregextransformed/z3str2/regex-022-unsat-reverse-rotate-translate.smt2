(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re ".") (re.* (str.to_re "q"))))))
(assert (= (str.len x) 2))
(assert (not (= x "qq")))
(assert (not (= x "q.")))
(check-sat)
