(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "O") (re.* (str.to.re "?"))))))
(assert (= (str.len x) 3))
(assert (not (= x "?OO")))
(assert (not (= x "??O")))
(assert (not (= x "?O?")))
(check-sat)
