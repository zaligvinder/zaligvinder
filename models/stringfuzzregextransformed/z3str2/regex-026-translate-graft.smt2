(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "c")) (str.to_re "'\\u{b}'")))))
(assert (str.in_re y (re.* (re.++ (str.to_re "c") (str.to_re "'\\u{b}'")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
