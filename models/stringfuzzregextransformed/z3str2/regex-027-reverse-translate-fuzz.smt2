(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "Z") (re.* (str.to_re "O"))))))
(assert (= (str.len x) 1))
(assert (not (= x "?-*a7")))
(assert (not (= x "?qO")))
(assert (not (= x "r?")))
(check-sat)
