(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "")) (str.to_re "a")))))
(assert (str.in_re y (re.+ (str.to_re "v"))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= 14 (str.to_int x)))
(check-sat)
