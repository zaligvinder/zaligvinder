(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.+ (str.to_re "t")) (str.to_re "g")))))
(assert (str.in_re y (re.+ (re.union (re.* (str.to_re "")) (str.to_re "0")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
