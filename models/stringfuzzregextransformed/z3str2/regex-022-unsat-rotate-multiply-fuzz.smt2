(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.+ (str.to_re "@")) (str.to_re "aa")))))
(assert (= (str.len x) 0))
(assert (not (= x "hbb")))
(assert (not (= x "b")))
(check-sat)
