(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "b") (re.* (str.to_re "L"))))))
(assert (= (str.to_int x) 6))
(assert (not (= x "bOav")))
(assert (not (= x "b")))
(assert (not (= x "I7_")))
(check-sat)
