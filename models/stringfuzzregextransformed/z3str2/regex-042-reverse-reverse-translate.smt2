(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "Z") (re.* (str.to_re "n")))))
(assert (str.in_re x (re.++ (str.to_re "Z") (re.++ (re.* (str.to_re "n")) (re.* (str.to_re "$"))))))
(assert (= 3 (str.len x)))
(check-sat)
