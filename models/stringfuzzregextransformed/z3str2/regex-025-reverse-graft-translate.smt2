(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "U") (re.* (str.to_re "n"))))))
(assert (str.in_re y (re.* (str.to_re "n"))))
(assert (= (str.len x) 7))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
