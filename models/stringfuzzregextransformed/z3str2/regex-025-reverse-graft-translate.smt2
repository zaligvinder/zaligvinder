(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "U") (re.* (str.to.re "n"))))))
(assert (str.in.re y (re.* (str.to.re "n"))))
(assert (= (str.len x) 7))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
