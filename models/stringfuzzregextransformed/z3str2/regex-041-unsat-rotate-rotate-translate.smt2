(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "Y") (re.* (str.to_re "=")))))
(assert (str.in_re x (re.++ (str.to_re "N") (re.* (str.to_re "N")))))
(assert (str.in_re x (re.++ (str.to_re "N") (re.* (re.* (re.++ (str.to_re "=") (str.to_re "Y")))))))
(check-sat)
