(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (= (str.len x) 12))
(assert (= x y))
(assert (str.in_re y (re.+ (re.range "2" ":"))))
(assert (str.suffixof "" x))
(check-sat)
