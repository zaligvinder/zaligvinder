(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "aaaa")) (str.to_re "bbbb")))))
(assert (= (str.len x) 12))
(assert (not (= x "aaaabbbbbbbb")))
(assert (not (= x "bbbbaaaabbbb")))
(assert (not (= x "aaaaaaaabbbb")))
(check-sat)
