(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (str.to_re "LLVVzz||")))
(assert (str.in_re x (re.* (str.to_re "LLVVzz||LLVVzz||"))))
(assert (> (str.len x) (str.len x)))
(assert (< 40 50))
(check-sat)
