(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "l") (re.* (str.to_re "i")))))
(assert (str.in_re x (re.++ (str.to_re "E") (re.* (str.to_re "E")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "E"))) (str.to_re "i"))))
(check-sat)
