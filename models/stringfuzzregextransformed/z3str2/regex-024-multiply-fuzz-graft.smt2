(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.+ (str.to_re "a")) (str.to_re "bb")))))
(assert (str.in_re y (re.* (re.++ (str.to_re "#") (re.+ (str.to_re "Ep"))))))
(check-sat)
