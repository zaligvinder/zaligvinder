(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "?") (re.* (str.to_re "W")))))
(assert (str.in_re x (re.++ (re.* (re.* (str.to_re "K"))) (str.to_re "W"))))
(assert (= (str.len x) 3))
(check-sat)
