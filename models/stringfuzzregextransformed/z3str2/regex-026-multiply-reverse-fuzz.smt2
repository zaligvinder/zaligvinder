(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "2") (re.* (str.to_re "a"))))))
(assert (str.in_re y (re.+ (re.++ (str.to_re "b") (re.* (str.to_re ""))))))
(assert (not (= x y)))
(assert (= (str.to_int x) (str.to_int y)))
(check-sat)
