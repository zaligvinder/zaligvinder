(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "[")) (str.to_re "z")))))
(assert (= (str.len x) 0))
(assert (not (= x "EIzz")))
(assert (not (= x "[")))
(assert (not (= x "[hz")))
(check-sat)
