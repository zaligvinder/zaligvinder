(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "[")) (str.to.re "z")))))
(assert (= (str.len x) 0))
(assert (not (= x "EIzz")))
(assert (not (= x "[")))
(assert (not (= x "[hz")))
(check-sat)
