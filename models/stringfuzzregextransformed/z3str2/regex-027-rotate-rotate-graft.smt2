(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "b"))))
(assert (= 3 (str.len x)))
(assert (not (= x "aab")))
(assert (not (= x "abb")))
(assert (not (= x "bab")))
(check-sat)
