(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "y")) (re.++ (re.* (str.to_re "[")) (re.* (str.to_re "y"))))))
(assert (str.in_re x (re.++ (str.to_re "Q") (str.to_re "Q"))))
(assert (= (str.len x) 3))
(check-sat)
