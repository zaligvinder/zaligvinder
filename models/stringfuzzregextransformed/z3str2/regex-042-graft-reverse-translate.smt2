(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "y")) (re.++ (re.* (str.to.re "[")) (re.* (str.to.re "y"))))))
(assert (str.in.re x (re.++ (str.to.re "Q") (str.to.re "Q"))))
(assert (= (str.len x) 3))
(check-sat)
