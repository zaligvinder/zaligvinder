(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "") (re.* (str.to_re "a"))))))
(assert (= (str.len x) 0))
(assert (not (= x """""P1aa")))
(assert (not (= x "tC3'' '''' ''")))
(assert (not (= x ";Ha")))
(check-sat)
