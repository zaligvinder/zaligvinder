(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "' '")) (str.to_re "h")))))
(assert (= (str.len x) 3))
(assert (not (= x "' 'hh")))
(assert (not (= x "h' 'h")))
(assert (not (= x "' '' 'h")))
(check-sat)
