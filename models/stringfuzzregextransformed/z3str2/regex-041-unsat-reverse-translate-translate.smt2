(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "V")) (str.to.re "h"))))
(assert (str.in.re x (re.++ (re.* (str.to.re "B")) (str.to.re "B"))))
(assert (str.in.re x (re.++ (re.++ (re.* (str.to.re "B")) (re.* (str.to.re "V"))) (str.to.re "h"))))
(check-sat)
