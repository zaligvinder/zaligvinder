(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "V")) (str.to_re "h"))))
(assert (str.in_re x (re.++ (re.* (str.to_re "B")) (str.to_re "B"))))
(assert (str.in_re x (re.++ (re.++ (re.* (str.to_re "B")) (re.* (str.to_re "V"))) (str.to_re "h"))))
(check-sat)
