(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "h")) (str.to_re "d"))))
(assert (str.in_re x (re.union (re.union (re.* (str.to_re "S")) (re.+ (str.to_re ""))) (str.to_re ""))))
(assert (= 2 (str.len x)))
(check-sat)
