(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.union (str.to_re "a") (re.* (str.to_re "b")))))
(assert (str.in_re x (re.++ (re.union (re.+ (str.to_re "b")) (str.to_re "")) (re.+ (str.to_re "c")))))
(assert (= (str.len x) 4))
(check-sat)
