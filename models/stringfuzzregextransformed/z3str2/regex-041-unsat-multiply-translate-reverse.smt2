(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "SS")) (str.to_re "OO"))))
(assert (str.in_re x (re.++ (re.* (str.to_re "vv")) (str.to_re "vv"))))
(assert (str.in_re x (re.++ (re.++ (re.* (str.to_re "vv")) (re.* (str.to_re "SS"))) (str.to_re "OO"))))
(check-sat)
