(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "u") (re.* (str.to_re "L"))))))
(assert (= (str.len x) 3))
(assert (not (= x "uLL")))
(assert (not (= x "uuL")))
(assert (not (= x "uLu")))
(check-sat)
