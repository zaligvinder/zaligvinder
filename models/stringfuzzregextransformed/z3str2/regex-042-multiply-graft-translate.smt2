(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "jj") (re.* (str.to_re "&&")))))
(assert (str.in_re x (re.++ (str.to_re "jj") (re.++ (re.* (str.to_re "&&")) (str.to_re ">>")))))
(assert (= (str.len x) 6))
(check-sat)
