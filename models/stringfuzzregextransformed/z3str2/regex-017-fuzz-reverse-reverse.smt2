(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "AB") (re.++ (str.to_re "$!Pbcd") (str.to_re "hAhw'{"))))))
(assert (= 1 (str.to_int x)))
(check-sat)
