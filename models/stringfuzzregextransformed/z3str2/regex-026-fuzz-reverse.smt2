(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (str.to.re "b") (re.+ (str.to.re "g"))))))
(assert (str.in.re y (re.* (re.union (re.* (str.to.re "a")) (str.to.re "L")))))
(assert (not (= x y)))
(assert (= (str.to.int x) (str.to.int y)))
(check-sat)
