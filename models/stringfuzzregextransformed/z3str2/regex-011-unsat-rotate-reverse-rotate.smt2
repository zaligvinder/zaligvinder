(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "dcba"))))
(assert (str.in_re y (re.* (str.to_re "dcba"))))
(assert (= (str.len x) 6))
(check-sat)
