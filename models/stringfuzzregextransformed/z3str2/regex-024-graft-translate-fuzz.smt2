(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (re.+ (str.to_re "H"))) (str.to_re "taPra")))))
(assert (str.in_re y (re.+ (re.++ (str.to_re "") (str.to_re "a&")))))
(check-sat)
