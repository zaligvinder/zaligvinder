(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (= (str.to.int x) 6))
(assert (= x y))
(assert (str.in.re y (re.* (re.range "V" "V"))))
(assert (str.contains "" x))
(check-sat)
