(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "E") (re.+ (str.to_re "v"))))))
(assert (str.in_re y (re.+ (re.union (str.to_re """") (re.* (str.to_re ""))))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
