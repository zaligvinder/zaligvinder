(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "123") (re.union (str.to_re "L'") (str.to_re "BRk"""))))))
(assert (= 5 (str.len x)))
(check-sat)
