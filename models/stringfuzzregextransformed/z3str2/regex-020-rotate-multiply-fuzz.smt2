(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (re.+ (str.to_re "bQ,")) (str.to_re "*Q")))))
(assert (= (str.len x) 2))
(assert (not (= x "a&=LOidPpPs`6W8")))
(assert (not (= x "`~RD+WaXU[]")))
(assert (not (= x "0l'mW)bb")))
(check-sat)
