(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "=") (re.* (str.to_re "O"))))))
(assert (= (str.to_int x) 1))
(assert (not (= x "Vu")))
(assert (not (= x "w=")))
(check-sat)
