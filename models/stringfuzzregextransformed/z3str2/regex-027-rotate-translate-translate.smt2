(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "C")) (str.to_re "r")))))
(assert (= (str.len x) 3))
(assert (not (= x "rrC")))
(assert (not (= x "rCC")))
(assert (not (= x "CrC")))
(check-sat)
