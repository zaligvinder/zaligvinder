(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "[")) (str.to_re "z")))))
(assert (= (str.len x) 3))
(assert (not (= x "[zz")))
(assert (not (= x "z[z")))
(assert (not (= x "[[z")))
(check-sat)
