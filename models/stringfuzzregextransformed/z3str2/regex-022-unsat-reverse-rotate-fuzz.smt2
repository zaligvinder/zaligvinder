(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (str.to_re "9") (re.+ (str.to_re "'\r'"))))))
(assert (= (str.len x) 0))
(assert (not (= x ")c")))
(assert (not (= x "ba")))
(check-sat)
