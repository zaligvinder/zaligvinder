(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (str.to_re ":") (re.+ (str.to_re "u"))))))
(assert (str.in_re y (re.* (re.++ (str.to_re "1") (re.* (str.to_re ""))))))
(check-sat)
