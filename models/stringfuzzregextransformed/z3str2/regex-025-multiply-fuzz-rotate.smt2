(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.* (str.to.re "UTb")) (str.to.re "P")))))
(assert (str.in.re y (re.+ (re.union (re.+ (str.to.re "")) (str.to.re "aa")))))
(assert (= (str.len x) (str.to.int y)))
(assert (not (= x y)))
(assert (= (str.len x) 6))
(check-sat)
