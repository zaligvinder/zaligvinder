(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (re.* (str.to_re "l")) (re.* (re.++ (str.to_re "m") (re.* (str.to_re "m")))))))
(assert (str.in_re x (re.++ (str.to_re "&") (str.to_re "&"))))
(assert (= (str.len x) 3))
(check-sat)
