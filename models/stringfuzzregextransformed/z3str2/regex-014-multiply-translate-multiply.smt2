(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "ppppcccc"))))
(assert (str.in_re y (re.* (str.to_re "ppppcccc"))))
(assert (= (str.len x) 8))
(assert (= (str.len y) 16))
(check-sat)
