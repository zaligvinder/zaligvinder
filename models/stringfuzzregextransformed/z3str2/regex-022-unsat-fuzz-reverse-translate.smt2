(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re "h")) (str.to_re "I")))))
(assert (= (str.to_int x) 3))
(assert (not (= x "II")))
(assert (not (= x "?")))
(check-sat)
