(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.++ (re.+ (str.to_re "&")) (re.union (re.* (str.to_re "&")) (str.to_re "I"))))))
(assert (str.in_re y (re.* (str.to_re ""))))
(assert (not (= x y)))
(assert (= (str.to_int x) (str.len y)))
(check-sat)
