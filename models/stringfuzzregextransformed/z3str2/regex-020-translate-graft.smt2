(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "L") (re.* (str.to.re "{"))))))
(assert (= 3 (str.len x)))
(assert (not (= x "{LL")))
(assert (not (= x "L{L")))
(assert (not (= x "{{L")))
(check-sat)
