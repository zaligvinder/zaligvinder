(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (re.* (str.to_re "M")) (str.to_re "J")))))
(assert (= (str.len x) 3))
(assert (not (= x "MMJ")))
(assert (not (= x "MJJ")))
(assert (not (= x "JMJ")))
(check-sat)
