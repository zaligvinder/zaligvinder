(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "aabb1122"))))
(assert (str.in_re y (str.to_re "aabb1122")))
(assert (= 8 (str.len x)))
(assert (= (str.len y) 16))
(check-sat)
