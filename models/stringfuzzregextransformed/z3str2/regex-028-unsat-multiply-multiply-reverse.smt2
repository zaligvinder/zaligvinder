(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (str.to_re "bbbbaaaa"))))
(assert (str.in_re x (re.* (str.to_re "bbbbaaaabbbbaaaa"))))
(assert (str.in_re x (re.* (str.to_re "ccccaaaabbbbaaaabbbbaaaa"))))
(assert (> (str.len x) 4))
(check-sat)
