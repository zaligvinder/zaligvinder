(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.+ (re.union (re.* (str.to_re ";;")) (str.to_re "DD")))))
(assert (str.in_re y (re.* (re.union (re.+ (str.to_re ";;")) (str.to_re "DD")))))
(check-sat)
