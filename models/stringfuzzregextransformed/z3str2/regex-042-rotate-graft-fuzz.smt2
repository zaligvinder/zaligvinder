(set-logic ALL)
(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in_re x (re.++ (str.to_re "D") (re.* (str.to_re "Y")))))
(assert (str.in_re x (re.union (re.* (re.* (str.to_re ""))) (str.to_re "t"))))
(assert (= (str.to_int x) 5))
(check-sat)
