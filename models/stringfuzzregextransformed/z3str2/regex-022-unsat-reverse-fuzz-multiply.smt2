(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.++ (str.to_re "bb") (re.+ (str.to_re ""))))))
(assert (= (str.len x) 8))
(assert (not (= x "DD")))
(assert (not (= x "<<")))
(check-sat)
