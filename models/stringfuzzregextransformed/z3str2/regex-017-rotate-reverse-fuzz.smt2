(set-logic ALL)
(declare-const x String)
(declare-const y String)
(assert (str.in_re x (re.* (re.union (str.to_re "k/X") (re.union (str.to_re "") (str.to_re "BC"))))))
(assert (= 2 (str.len x)))
(check-sat)
